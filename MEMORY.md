# 协作记忆

> ⚠️ 这份文档在**公开仓库**里。别往里写凭据、真实手机号、老师姓名、兑换码。

## 这份文档管什么（跟另外两份别混）

| 文档 | 管什么 | 判据 |
|---|---|---|
| `CLAUDE.md` | **产品和代码**：定死的决策、容易犯的错、当前状态 | 「这个东西该怎么做」 |
| `docs/handoff/*.md` | **某一轮做了什么**，带日期，写完就不再改 | 「那一轮发生了什么」 |
| **这份** | **怎么跟这个人合作**：他的偏好、他反悔过什么、他叫我别做什么 | 「跟他打交道要注意什么」 |

一件事只写在一处。拿不准就问：删掉它，是「不知道该怎么做」还是「不知道该怎么沟通」？
前者进 CLAUDE.md，后者留这儿。

---

## 他是谁

- **产品负责人，不是专业开发者。** 说清楚「为什么」比「怎么做」重要。
  他明确要求过「用更朴素的话说」，**少用术语**
- 要发论文。所以可追溯的研究数据这些事对他是硬约束
  （⚠️ 但 2026-08-30 他决定**不把伦理审查批号设为上线闸门**，改成知情同意告知。
  代价我已如实写进 ADR-002，**别再拿这条劝他**）
- **界面效果他自己看，我看不到全部。** 每次交付都要如实说「好不好看得你自己看」
- 全程中文沟通

---

## 常驻要求（每轮都适用）

1. **每轮交付给一张 summary table**（做了什么 / 还差什么），然后**单独说清「你需要做什么」**。
   2026-08-20 他明确提的。理由：待办分散在几份文档里，他要的是一眼看到全局，
   不是读一段散文自己拼出结论。`CLAUDE.md` 的「当前状态」开头那张表就是它，
   **改完东西记得改它**
2. **界面上不写解释性小字。** 这是他提得最多的一条（至少七次）。
   判据：**她不看这句话会做错什么吗？** 答不出来就删。只有两种留得住 ——
   边界说明（「超管可见全号」）、不可逆动作的后果（注销那两次确认）
3. **不要用多 agent / Workflow**，除非他自己说
4. **不主动提交或 push**，他要的时候会说
5. **改后端代码必须重启，而且要确认真的重启了。**
   `admin.bat` 检测到 3000 上已有后端时**只开浏览器、不起新进程** ——
   所以「双击了、后台也打开了」完全可能还跑着旧代码。
   正确顺序：先在那个窗口 Ctrl+C，再双击。
   **不要凭「我改完了」就说「好了」** —— 这一类的表现是改完毫无变化，
   然后去查代码，而代码是对的

   🔴 **不要给 admin.bat 加 `--watch`**。2026-08-20 加过，当天撤了：
   任务队列在内存里，而生成教案 20-30 秒、配图 20-70 秒 ——
   `--watch` 在**任务进行中**开火，实测入队一秒后就把它杀了

---

## 跟他讨论方案的方式

**他给的方案往往可行，但有一处结构性问题。**「接方案 + 指出那一处」比全盘否定或全盘照做都好。
已经发生过的例子：

- 他想把兑换码绑到手机号上 → 那会让问卷星自动发码那条路断掉
- 他说「跳转前那个『去看教案』没必要，删掉」→ 正常路径上确实没必要，
  但 `lesson_plan_id` 拿不到时它是那条路唯一的出口。所以按条件保留，并说明
- 2026-08-30 他提「园所给手机号白名单，老师直接进」→ 拆分「账号存在」和
  「有没有额度」是对的、照做了；但拿手机号当没验证的钥匙意味着**园长能登进
  任何一位老师的账号**，而那正是协议里加粗承诺不会发生的事。指出后他自己换成了密码方案

**他会推翻自己上一个决定。** 遇到反转不要急着实现 ——
**先把新方案的代价说清楚，他接受了再动**。

**他会主动问「这样行不行」并要我给思路。** 这种时候他要的是设计，不是选项 ——
给一个完整方案 + 它的代价，比列三个选项让他选更有用。

---

## 已经反悔过的事（不要主动重开）

| 事 | 结论 | 什么时候定的 |
|---|---|---|
| **做小程序还是 web** | **web**。理由是甩掉「借企业主体」，**不是免备案**（备案反而回来了） | 2026-08-30（ADR-002） |
| **身份模型** | 反转三次。08-19 落在「兑换码 + 从名单选自己」、手机号删掉；**08-30 又反转成手机号 + 密码** | 2026-08-30 |
| **手机号要不要收** | **收**，但只当登录用户名。08-19 说不收、08-30 反转 | 2026-08-30 |
| **伦理审查批号** | **不设为上线闸门**，改知情同意告知 | 2026-08-30 |
| 兑换码当登录钥匙 | 放弃（他的原话：太麻烦，且兑换码一直在换） | 2026-08-30 |
| 挂靠朋友公司的域名 | 放弃，自己注册 + 个人备案 | 2026-08-30 |
| 左侧序号竖向刻度轨 | 拆掉（刻度硬编码绝对定位，跟右边内容永远对不齐） | 2026-08-17 |
| 一次只问一题 | 改成一屏 4 题 | 2026-08-17 |
| 独立「设置」页 | 整个撤掉，三行内容摆在「我的」底部 | 2026-08-18 |
| 老师选配图模型 | 老师完全不碰，全在管理后台 | 2026-08-18 |
| 首页顶部风景插画 | 撤掉，输入框改成纵向居中 | 2026-08-20 |
| 教案库「点了就删」 | 加回二次确认（删教案不可逆） | 2026-08-25 |
| 教师详情显示兑换码 | 撤掉（推翻了 08-22 留它的理由；编号还留着） | 2026-08-25 |

---

## 我犯过的错（同一类别再犯就是没长记性）

- **报「没有数据」之前先确认查的是不是对的字段。** 概览那句 SQL 查的键名跟真实值
  差一个前缀 —— 唯一重要的指标静静地显示零，而且看起来完全正常
- **规则写在文档里不等于生效。** design-tokens 的「配白字用 deep 档就达标」写了一年，
  而旧 `$mint-deep` 配白字只有 3.97:1。现在 `test:contrast` 逐对算
- **别在失败的时候画成空。** 任务页把「拉不到」渲染成「现在没有可以做的事」，
  「我的」渲染成 `0/0 次教案`。假话的代价是她不会重试，她会走
- **不确定的话一个字都别说。** 断网那一屏第一版写着「网回来了」，而网是断的
- **回归断言里不许有会随轮次变红的绝对数字。** 红两次之后整份脚本就没人看了
- **报「坏了」之前先确认那东西真的存在。** 我自己的交接文档写着
  「档案编辑和成稿编辑界面上有入口、点了没反应」，去核才发现**那两个入口根本不存在**。
  「有入口但点不动」要修，「压根没入口」要做 —— 按前者去修会交付一个他看不见的东西。
  **上一轮的自己也是二手信源**，落地前照样要核
- **白名单和硬校验同类：生效和不生效，正常输入下输出一模一样。** 写它的时候就得
  同时写一条「喂它白名单外的东西」的断言，否则那道过滤哪天空转了没人知道
- 🔴 **问他需求时用他在屏幕上看得见的东西描述，不要用我给字段起的名字。**
  2026-08-21 我问「导出时解读要不要一起导」，他答「要」，做完他才说
  他以为「解读」指的是**设计意图**（本来就在导出里）。整件事白做加撤回。
  该问的是「成稿页那些折叠的『为什么这样设计』要不要进导出的文件」
- **他改文案偏好直白**：「算了」→「取消」、「二三十秒」→「20-30 秒」。
  口语的、带情绪的、约数的写法他都会挑出来
- 🔴 **他说「对齐」时先问清是跟哪一条边。** 一列带编号的列表有**两条**左边界
  （编号列 / 正文），我选错了 —— 改完视觉结果跟原来一模一样，他只能再提一次。
  同类的还有「居中」「等宽」「对齐上一行」
- 🔴 **改完要量「跟上一版比变了吗」，不只量「现在对不对」。**
  我把空元素占位换成 `padding-left:44rpx` —— 两者结果完全相同，那次改动是空转的
- 🔴 **他说的理由可能跟事实相反，要当场核。** 2026-08-30 他说转 web 是因为
  「小程序备案太麻烦」，而项目里唯一一条免备案的路正是小程序云托管 ——
  转 web 反而把备案请了回来。核完之后真实理由才浮出来（甩掉企业主体），
  而 ADR 里写对理由这件事很重要：**理由写错的 ADR，半年后会让人把决定推翻回来**
- **他给的方案要先查现实约束再评价。** 他问「手机号 + 短信验证码行不行」，
  查了才知道腾讯云 2025-09-18 起个人不能报备签名 —— 那条路会把企业主体请回来。
  **没查之前不要下结论，也不要凭印象说「可以」**

---

## 环境与协作路径

- **要他自己跑命令时**，让他在对话里输 `! <命令>` —— 输出会直接进对话
- 🔴 **界面的事我能自己量，不用全靠他看。** 起 dev server，用 chrome-devtools 打开、
  `getBoundingClientRect()` 量坐标、`el.click()` 测交互、截图。
  **「对不齐」这类问题量一次就有数**，比来回问快得多
  - ⚠️ **端口不一定是 5173**，被占了 vite 会换到 5174。看它自己那行输出
  - 完整配方（造已激活账号、塞 token、**必须 reload 不能 navigate 同一个 hash**）在
    `docs/handoff/2026-08-21-界面打磨与浏览器实测工作流.md`
- **给他指元素看**：让他开 dev server（跑的是真源码，DOM 类名跟 `.vue` 文件一一对应）。
  **不是** `prototype/index.html` —— 那是一份手写镜像，在它上面指元素我改不到任何代码
  - 他用的 Orca 是个 Electron 应用，没注册文件关联，`.html` 双击走的是 Edge。
    要在 Orca 里看就在它的内嵌浏览器地址栏粘 URL，别指望双击文件
- 一键起后端 + 开后台：双击根目录 `admin.bat`。**那个窗口就是后端本体**，没开 `--watch`
  - 我要跑回归但不想动他那个窗口：`PORT=3100 node src/server.js` 另起一个，
    脚本传 `API_BASE=http://localhost:3100`（`mode-test.mjs` 和 `smoke.js` 那个变量名是 `BASE`）。
    跑完记得把它关掉
- 本地开发管理员账号见 `CLAUDE.md`；真实凭据在 `backend/.env`（已 gitignore）

---

## 他的其它项目（别混）

他同时在做 **Hualong**（`D:\hualong-teacher` + `hualong-backend`），
那是另一个微信小程序，2026-08-26 起在把一套 60 页的 iframe HTML 网页原型迁进小程序。
**跟 STEM 这个项目没有任何共享代码**。他偶尔会在两个项目之间记混。

---

## 悬着的事（他要拍板或只能他做）

**只能他做的**：

- 域名注册 + **个人 ICP 备案** + HTTPS 证书（**上线闸门**，见 ADR-002）
- 腾讯云轻量应用服务器 + COS 开通
- 腾讯云文本内容安全（TMS）开通（个人实名即可）
- **找真实幼儿园老师复核教案**（产品最大的未知数，至今零复核）
- 轮换 12ai 的 key（更早的 session 里贴过明文）

**要他拍板的**：积分转让（会动额度台账的语义模型，形状还没想清楚）、下一步做哪个。

**上线前别忘**：`IMAGE_DAILY_LIMIT` 从 100（本机联调值）调回 10、
管理后台那个本地开发账号必须换掉（后台能看到全部老师的对话内容）。
