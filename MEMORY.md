# 协作记忆

> ⚠️ 这份文档在**公开仓库**里。别往里写凭据、真实手机号、老师姓名、兑换码。

## 这份文档管什么（跟另外两份别混）

| 文档 | 管什么 | 判据 |
|---|---|---|
| `CLAUDE.md` | **产品和代码**：定死的决策、容易犯的错、当前状态 | 「这个东西该怎么做」 |
| `docs/handoff/*.md` | **某一轮做了什么**，带日期，写完就不再改 | 「那一轮发生了什么」 |
| **这份** | **怎么跟这个人合作**：他的偏好、他反悔过什么、他叫我别做什么 | 「跟他打交道要注意什么」 |

一件事只写在一处。拿不准就问：删掉它，是「不知道该怎么做」还是「不知道该怎么沟通」？
前者进 CLAUDE.md，后者留这儿。

---

## 他是谁

- **产品负责人，不是专业开发者。** 说清楚「为什么」比「怎么做」重要。
  他明确要求过「用更朴素的话说」，**少用术语**
- 要发论文。所以伦理审查、可追溯的研究数据这些事对他是硬约束，不是锦上添花
- **他在微信开发者工具里看效果，我看不到。** 每次交付都要如实说「好不好看得你自己看」
- 全程中文沟通

---

## 常驻要求（每轮都适用）

1. **每轮交付给一张 summary table**（做了什么 / 还差什么），然后**单独说清「你需要做什么」**。
   2026-08-20 他明确提的。理由：待办分散在六份文档里，他要的是一眼看到全局，
   不是读一段散文自己拼出结论。`CLAUDE.md` 的「当前状态」开头那张表就是它，**改完东西记得改它**
2. **界面上不写解释性小字。** 这是他提得最多的一条（到 2026-08-18 已经第四次）。
   判据：**她不看这句话会做错什么吗？** 答不出来就删。只有两种留得住 ——
   边界说明（「超管可见全号」）、不可逆动作的后果（注销那两次确认）
3. **不要用多 agent / Workflow**，除非他自己说
4. **不主动提交或 push**，他要的时候会说
5. **改后端代码必须重启，而且要确认真的重启了。**
   `admin.bat` 检测到 3000 上已有后端时**只开浏览器、不起新进程** ——
   所以「双击了、后台也打开了」完全可能还跑着旧代码（他 2026-08-20 就碰到这个）。
   正确顺序：先在那个窗口 Ctrl+C，再双击。
   核实一句：`wmic process where "ProcessId=<pid>" get CommandLine`。
   **不要凭「我改完了」就说「好了」** —— 这一类的表现是改完毫无变化，
   然后去查代码，而代码是对的。

   🔴 **不要给 admin.bat 加 `--watch`**。2026-08-20 加过，当天撤了：
   任务队列在内存里（`services/taskQueue.js` 文件头第一行就写着「进程重启，
   排队中和执行中的任务会丢」），而生成教案 20-30 秒、配图 20-70 秒 ——
   `--watch` 在**任务进行中**开火，实测入队一秒后就把它杀了，图片行永远停在 pending。
   要边改边跑就另开终端 `npm run dev`，但别在生成时保存后端文件。

---

## 跟他讨论方案的方式

**他给的方案往往可行，但有一处结构性问题。**「接方案 + 指出那一处」比全盘否定或全盘照做都好。
已经发生过的例子：

- 他想把兑换码绑到手机号上 → 那会让问卷星自动发码那条路断掉
- 他说「跳转前那个『去看教案』没必要，删掉」→ 正常路径上确实没必要，
  但 `lesson_plan_id` 拿不到时它是那条路唯一的出口。所以按条件保留，并说明

**他会推翻自己上一个决定。** 遇到反转不要急着实现 —— **先把新方案的代价说清楚，他接受了再动**。

---

## 已经反悔过的事（不要主动重开）

| 事 | 结论 | 什么时候定的 |
|---|---|---|
| **身份模型** | 反转两次，最后落在「兑换码 + 从名单里选自己是哪一位」，手机号从库里彻底删掉 | 2026-08-19（013→016 迁移） |
| **手机号要不要收** | 现在不收。形状想清楚了写在 `CLAUDE.md` 待定项里，**想清楚之前不要重新讨论** | 2026-08-19 |
| 左侧序号竖向刻度轨 | 拆掉（刻度硬编码绝对定位，跟右边内容永远对不齐） | 2026-08-17 |
| 一次只问一题 | 改成一屏 4 题 | 2026-08-17 |
| 独立「设置」页 | 整个撤掉，三行内容摆在「我的」底部 | 2026-08-18 |
| 老师选配图模型 | 老师完全不碰，全在管理后台 | 2026-08-18 |
| 首页顶部风景插画 | 撤掉，输入框改成纵向居中。`illoHero()` 代码留着没删 | 2026-08-20 |

---

## 我犯过的错（同一类别再犯就是没长记性）

- **报「没有数据」之前先确认查的是不是对的字段。** 概览那句 SQL 查 `kind = 'rating'`，
  真实值是 `'lesson_rating'` —— 唯一重要的指标静静地显示零，而且看起来完全正常
- **规则写在文档里不等于生效。** design-tokens 的「配白字用 deep 档就达标」写了一年，
  而旧 `$mint-deep` 配白字只有 3.97:1。现在 `test:contrast` 逐对算
- **别在失败的时候画成空。** 任务页把「拉不到」渲染成「现在没有可以做的事」，
  「我的」渲染成 `0/0 次教案`。假话的代价是她不会重试，她会走
- **不确定的话一个字都别说。** 断网那一屏第一版写着「网回来了」，而网是断的
- **回归断言里不许有会随轮次变红的绝对数字。** 红两次之后整份脚本就没人看了
- **查「点了没反应」先读编译产物**（`frontend/dist/dev/mp-weixin/`），不是读源码猜
- **`test:mp` 默认查 `dist/dev`。** 刚跑完 `build` 要显式指 `dist/build/mp-weixin`，
  否则查的是旧产物、全绿也没意义

---

## 环境与协作路径

- **要他自己跑命令时**，让他在对话里输 `! <命令>` —— 输出会直接进对话
- **给他指元素看**：让他开 `http://localhost:5173`（H5 dev server，跑的是真源码，
  DOM 类名跟 `.vue` 文件一一对应）。**不是** `prototype/index.html` ——
  那是一份手写镜像，在它上面指元素我改不到任何代码
  - 他用的 Orca 是个 Electron 应用，没注册文件关联，`.html` 双击走的是 Edge。
    所以要在 Orca 里看就在它的内嵌浏览器地址栏粘 URL，别指望双击文件
  - H5 看不出「只在微信里出问题」那一类（点了没反应、WXML 编译错误），那种得他开微信工具
- 一键起后端 + 开后台：双击根目录 `admin.bat`
- 本地开发管理员账号见 `CLAUDE.md`；真实凭据在 `backend/.env`（已 gitignore）

---

## 悬着的事（他要拍板或只能他做）

**只能他做的**：ICP 备案 + 小程序 AppID（`msgSecCheck` 至今一次都没真调通）、
伦理审查批号（名单进真名的前提，**事后补不上**）、找真实幼儿园老师复核教案
（产品最大的未知数，至今零复核）、轮换 12ai 的 key（更早的 session 里贴过明文）。

**要他拍板的**：积分转让（会动额度台账的语义模型，形状还没想清楚）、
字号「特大」到正文 20px 够不够、下一步做哪个。

**上线前别忘**：`IMAGE_DAILY_LIMIT` 从 100（本机联调值）调回 10、
管理后台那个本地开发账号必须换掉（后台能看到全部老师的对话内容）。
