# ⚠️ 体验版过渡配置（2026-08-25）：线上域名还没定（ICP 备案未办），
# 而体验版需要一个能连的后端，所以这里临时指向开发机的局域网 IP。
# 体验者要满足两个条件才连得上：连着这台电脑所在的 WiFi + 在小程序里打开「开发调试」
# （体验版默认校验合法域名，http:// 裸 IP 过不了那道检查）。
#
# 🔴 上线前必须改回来：VITE_API_BASE 换成备案过的 https 域名，
#    VITE_DEV_FAKE_LOGIN 改回 false（并且后端要配好 AppSecret，DEV_FAKE_LOGIN=false）。
#    体验版曾因为这里是空串而「网络好像断了」—— 空地址的包一切正常地编译、上传、
#    通过审核流程，只在打开的那一刻连不上任何东西。
# 🔴 这个 IP 跟着网络走，换网必改（同 .env.development 里那条）。
VITE_API_BASE=http://192.168.2.104:3000/v1

# 后端 DEV_FAKE_LOGIN=true 时体验版也走假登录。真 AppSecret 配好之前只有这条路能登进去。
VITE_DEV_FAKE_LOGIN=true
