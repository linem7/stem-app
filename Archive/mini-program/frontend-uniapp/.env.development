# 本地开发。后端 npm start 起在 3000，路由同时挂了 /v1 和 /，这里用显式的 /v1。
# 小程序访问 http:// 地址需要在微信开发者工具里勾上「不校验合法域名」。
#
# 🔴 这里写**电脑的局域网 IP**，不写 localhost（2026-08-25 真机调试撞出来的）：
#    包是在手机上跑的，localhost 指的是手机自己 —— 开发者工具里一切正常、
#    一上真机就「网络好像断了」。
# 🔴 这个 IP 跟着网络走，**换网必改**（当天就撞了第二次：先在手机热点下写成
#    172.20.10.4，几分钟后切回 WiFi，那张网卡整个没了 —— 手机往一个不存在的
#    地址发请求，没人拒绝也没人回应，表现是「等太久了」而不是「网断了」）。
#    真机连不上时第一件事：`ipconfig` 对一下这个数，手机和电脑必须在同一个网里。
#    ⚠️ 改完必须 Ctrl+C 重启 dev:mp-weixin —— watcher 不会自己重读 .env。
VITE_API_BASE=http://192.168.2.104:3000/v1

# H5 预览专用：走 vite 的 dev 代理（同源），绕开后端没有 CORS 这件事。
# 只有 H5 读它（utils/env.js 里用 `#ifdef H5` 分流），小程序读上面那个绝对地址。
# 🔴 别改回「在 .env.development.local 里覆盖 VITE_API_BASE」那种做法 ——
#    Vite 对 mode=development 一律加载 .local 且优先级最高，两个平台会被一起改掉，
#    而小程序拿到相对路径就连不上后端，且不报有用的错。
VITE_H5_API_BASE=/v1

# 后端 .env 里 DEV_FAKE_LOGIN=true 时，用 code:"dev:任意字符串" 就能登录，
# 不需要真 AppID。上线前这里要关掉，走真正的 wx.login。
VITE_DEV_FAKE_LOGIN=true
