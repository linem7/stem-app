# 前端环境变量。开发时用 .env.development（已提交，值都不是秘密）。
#
# 🔴 这里不许出现任何密钥。API key 只在服务端 —— 前端产物是可以被打开看的。

# 后端地址。不配就是 /v1（相对路径），两种场景都对：
#   开发   浏览器打到 vite，vite 代理转给后端
#   上线   前端产物和后端同域名同端口
# 只有前后端不同源时才需要写成完整地址，那时后端也得开 CORS。
VITE_API_BASE=/v1

# 开发期假登录。**上线的那份 .env 里不许有这一项。**
VITE_DEV_FAKE_LOGIN=true

# 假登录用哪个身份。由 `npm run dev:account` 写进 .env.development，不用手改。
# **上线的那份 .env 里同样不许有这一项。**
VITE_DEV_OPENID=

