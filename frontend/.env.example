# 前端环境变量。开发时用 .env.development（已提交，值都不是秘密）。
#
# 🔴 这里不许出现任何密钥。API key 只在服务端 —— 前端产物是可以被打开看的。

# 后端地址。不配就是 /v1（相对路径），两种场景都对：
#   开发   浏览器打到 vite，vite 代理转给后端
#   上线   前端产物和后端同域名同端口
# 只有前后端不同源时才需要写成完整地址，那时后端也得开 CORS。
VITE_API_BASE=/v1

# 原来这里还有 VITE_DEV_FAKE_LOGIN / VITE_DEV_OPENID 两项开发期假登录。
# 手机号 + 密码落地之后（2026-09-20）删掉了 —— 前端**只有 API_BASE 一个环境变量**，
# 而且它只在前后端不同源时才需要改。本地和线上现在是同一条路。

