# 本地开发。vite dev server 把 /v1 代理给 localhost:3000（见 vite.config.js）。
VITE_API_BASE=/v1

# 登录改成手机号 + 密码之后（2026-09-20），这里就没有别的东西了 ——
# 原来那两项 VITE_DEV_FAKE_LOGIN / VITE_DEV_OPENID 随后端的假登录一起删了。
#
# 本地怎么进：浏览器打开首页，没登录会落到 /redeem。
# 先用后台建一个兑换码、导一段名单，再拿那个码在页面上激活一个账号。
# 之后就用手机号 + 密码，跟线上一样。
