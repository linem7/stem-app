# 设计令牌 · Design Tokens

> 2026-08-16 用户提供配色参考图并指定采用。本文件是**唯一色彩来源**，设计稿与小程序代码共用同一套令牌。改色只改这里。

---

## 1. 色板

### 底色

| 令牌 | Hex | oklch | 用途 |
|---|---|---|---|
| `--canvas` | `#F9F5E3` | `oklch(0.966 0.028 100)` | 展板/外层画布的淡奶油黄 |
| `--paper` | `#FFFDF8` | `oklch(0.993 0.008 90)` | 屏幕主底，近白暖奶油 |
| `--paper-2` | `#FBF7EC` | `oklch(0.975 0.016 92)` | 次级分区底 |

### 主色 · 暖阳黄（主行动）

| 令牌 | Hex | oklch | 用途 |
|---|---|---|---|
| `--amber` | `#F5C63D` | `oklch(0.845 0.145 88)` | **主按钮底**、选中态、草稿标记、进度条 |
| `--amber-soft` | `#FDF6DE` | `oklch(0.972 0.040 92)` | 选中项浅底、草稿卡底 |
| `--amber-line` | `#E8B62A` | `oklch(0.795 0.150 85)` | 描边、分隔 |
| `--amber-deep` | `#8A6410` | `oklch(0.50 0.105 80)` | **黄色系文字**（黄本身太亮，做文字必须用这一档） |

### 次色 · 薄荷绿（次级行动 / 已完成）

| 令牌 | Hex | oklch | 用途 |
|---|---|---|---|
| `--mint` | `#6FBF8B` | `oklch(0.73 0.105 155)` | 已完成圆点、筛选选中、浅底按钮 |
| `--mint-deep` | `#3E8F5D` | `oklch(0.575 0.115 155)` | **配白字的绿按钮底**（如「生成配图」）、绿色文字 |
| `--mint-soft` | `#EAF6EE` | `oklch(0.955 0.030 158)` | 浅绿底 |
| `--mint-line` | `#CFE7D8` | `oklch(0.895 0.038 158)` | 浅绿底上的描边（与 `--amber-line` 同位） |

### 点缀 · 天空蓝 / 珊瑚粉

| 令牌 | Hex | oklch | 用途 |
|---|---|---|---|
| `--sky` | `#8FC7E8` | `oklch(0.79 0.075 235)` | 插画天空、中性提示带、STEAM 的 T |
| `--sky-soft` | `#EAF3FA` | `oklch(0.955 0.020 235)` | 极浅蓝底 |
| `--sky-deep` | `#2E6E96` | `oklch(0.52 0.095 235)` | 蓝色文字 |
| `--coral` | `#EE8069` | `oklch(0.72 0.125 35)` | 「我的」tab、STEAM 的 A、少量强调 |
| `--coral-deep` | `#B44B33` | `oklch(0.52 0.135 33)` | 珊瑚色文字 |

### 中性

| 令牌 | Hex | oklch | 用途 |
|---|---|---|---|
| `--ink` | `#3A3630` | `oklch(0.32 0.012 80)` | 正文·暖褐黑 |
| `--ink-2` | `#6B655C` | `oklch(0.48 0.014 82)` | 次级文字 |
| `--ink-3` | `#7A7368` | `oklch(0.52 0.014 82)` | 最浅可用文字，不得再浅 |
| `--rule` | `#EBE5D6` | `oklch(0.925 0.016 92)` | 分隔线 |
| `--white` | `#FFFFFF` | — | 卡片实底、deep 档按钮上的文字 |

### 派生档（2026-08-17 补）

不是新色相，是上面几个色为了具体状态派生出来的档。写前端时发现组件里躲不开这几个值，
按「组件里禁止裸 hex」的约定，它们必须在这份文档里有名字。

| 令牌 | Hex | 从哪来 | 用途 |
|---|---|---|---|
| `--amber-shadow` | `#E8B62A` | 同 `--amber-line` | 主按钮下沿那道实边 |
| `--mint-shadow` | `#35784F` | `--mint-deep` 再压暗一档 | 绿按钮下沿那道实边 |
| `--disabled-bg` | `#EFEADE` | `--rule` 提亮 | 禁用态按钮底 |
| `--disabled-ink` | `#948D81` | `--ink-3` 提亮 | 禁用态按钮字 |

⚠️ `--disabled-ink` 在 `--disabled-bg` 上只有约 3.0:1，**不到正文的 4.5:1**。
这是有意的：禁用态本来就该读起来费劲一点，让人先去处理「为什么按不了」。
但因此**禁用不能只靠变灰**——按钮旁边必须同时有一句话说明还差什么
（比如「先选年龄班」），这是规则 3 在禁用态上的延伸。

---

## 2. 色彩使用规则

### 规则 1 · 亮色只做底，深色只做字
`--amber` / `--mint` / `--sky` / `--coral` 四个亮色**明度都在 0.72–0.85**，它们：
- ✅ 可以做**大面积底**，配 `--ink` 文字（对比度约 7–9:1）
- ❌ **不可以做文字色**（在奶油底上只有 1.5–3:1）
- 需要彩色文字时，一律用对应的 `-deep` 档

### 规则 2 · 白字必须配 deep 档
按钮若用白字，底色必须用 `--mint-deep` / `--sky-deep` / `--coral-deep` / `--amber-deep`。
`--amber` 和 `--mint` 本体配白字都不达标（分别约 1.9:1 和 2.4:1）。

**推荐做法**：主按钮用 `--amber` 底 + `--ink` 字（约 8.4:1），比白字方案更亮也更清楚。

### 规则 3 · 颜色不做状态的唯一载体
「草稿 / 已完成」这类状态，颜色之外**必须同时有文字或图形**。理由：
- 黄色在奶油底上先天到不了 3:1
- 对色觉障碍用户友好

### 规则 4 · 功能与色相的固定绑定
| 语义 | 色 |
|---|---|
| 主行动（开始 / 下一题 / 继续） | 暖阳黄 `--amber` |
| 次级行动、已完成、成功 | 薄荷绿 `--mint` / `--mint-deep` |
| 草稿、进行中 | 暖阳黄 `--amber` + 文字标注 |
| 中性提示、信息 | 天空蓝 `--sky-soft` |
| 个人 / 少量强调 | 珊瑚粉 `--coral` |
| 危险 / 删除 | `--coral-deep` |

一个语义只绑一个色，不许混用。

---

## 3. 圆角

| 令牌 | 值 | 用途 |
|---|---|---|
| `--r-chip` | 999px | chip、徽标、状态胶囊 |
| `--r-btn` | 16px | 按钮 |
| `--r-card` | 18px | 卡片 |
| `--r-input` | 22px | 输入框 |
| `--r-sm` | 10px | 小构件 |

## 4. 字体

```css
font-family: -apple-system, BlinkMacSystemFont, "PingFang SC",
             "Microsoft YaHei", "Source Han Sans SC", sans-serif;
font-synthesis: none;
```

不使用宋体（Windows 小字发虚，且与明亮气质不符）。层级靠字重（400/500/600/700）与字号建立，大标题加 `-0.012em` 字距收紧。

| 用途 | 字号 | 字重 |
|---|---|---|
| 页面大标题 | 22–24px | 700 |
| 焦点问题标题 | 19–20px | 600 |
| 卡片标题 | 16–17px | 600 |
| 正文 | 15px | 400 |
| 次级说明 | 13px | 400 |
| 标签/徽标 | 12px | 500 |

**下限**：正文 ≥14px，辅助文字 ≥12px，任何文字对比度 ≥4.5:1。

---

## 5. 插画规范

用户参考图中首页顶部有一幅扁平风景插画（太阳 / 云 / 小山 / 树 / 房子），采纳。

**做法约束**：
- 纯 inline SVG 手搓，只用**几何形**（圆、弧、三角、圆角矩形）
- 用本色板的黄 / 绿 / 蓝 / 珊瑚，不引入新色
- ❌ 不画人物面孔、不画写实细节、不用 AI 生图
- 出现位置克制：目前只用于首页顶部和空状态

理由：扁平几何风景是可以手工精确绘制的；一旦涉及人物或写实场景，手搓 SVG 必然劣化，那时应该留诚实占位或改用真实素材。

---

## 6. 落地到小程序

这套令牌应写成一份 `styles/tokens.wxss`（或 uni-app 的 `uni.scss`），全局引入。
组件里**禁止出现裸 hex 值**，一律引用变量——改配色时只改一处。

---

**版本**：1.0 · 2026-08-16
**来源**：用户提供的配色参考图
