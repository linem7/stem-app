# 运营体系：身份、额度与反馈

> 2026-09-23 对照 Web 身份与额度实现整理。当前待办见 [CLAUDE.md](../../CLAUDE.md)，接口字段见 [API 约定](api-spec.md)。早期运营决定已被 ADR-004 扩展。

## 0. 一句话模型

首次使用需要有效兑换码，教师可从名单选择身份，也可选择名单外自填。兑换码提供使用资格与额度，完善信息另有一次性奖励。

## 1. 身份与登录

首次流程：有效码 → 选择地区、园所、名单岗位，或「不在名单之内」自填 → 手机号输入两次并设置密码 → 激活与发放额度 → 同意协议后使用。

- 码不限定园所，绑定码的园所字段仅用于运营跟进。名单地区来自已有园所，自填地区使用内置全国省市表，自填来源记为 `entry_source=self`。
- 查名单必须先校验有效码，只显示姓氏。名单选择是身份信息，不是登录凭证。
- 手机号仅作为用户名，不用于联系，不返回教师页面，不进入模型提示词或日志；密码存 scrypt 哈希与盐。
- 已有账号用手机号密码登录。token 默认 30 天；`GET /me` 在剩余不足一半时续期。
- 退出清理当前浏览器登录与页面状态，同步同浏览器标签页；不删除数据，不影响其他设备凭证。有未保存输入时可取消。
- 微信换绑已退出当前流程，遗留表及后台接口不等于 Web 账号找回功能。超管重置密码与修改登录手机号仍是待实现需求，不能写成已可操作。

### 人、位置与账号

| 层 | 字段 | 用途 |
|---|---|---|
| 人 | `teacher_roster.teacher_ref` | 跨班追踪教师 |
| 位置 | `teacher_roster.id` | 人 × 园所 × 班级 × 岗位；换班新建位置，旧位置保留 |
| 账号 | `teachers.id` | 登录及业务数据归属，通过 `roster_entry_id` 关联位置 |

自填来源也创建名单身份行以保留研究编号。新 Web 账号不写 openid，不能靠遗留 `claimed_openid` 判断当前认领者。注销释放位置，但保留需要的研究及台账关系。

### 兑换码与完善信息奖励

- 首次激活校验全部通过后才消耗码、建立身份和发放额度；失败不得消耗码。
- 已登录教师续兑只增加额度，不更改身份；一个码只能兑一次，未兑码可作废。
- `POST /me/profile` 完善出生年月、学历、职称、教龄和任教年级后奖励 10 教案 + 5 配图，只能领取一次。
- 防重复依据是 `quota_grants` 中 `reason='完善信息'` 的记录；普通 `PATCH /me` 改档案不发奖励。
- 任务奖励通过运营发码、教师自行续兑完成；不把“每一笔额度必有兑换码”当作当前不变量。

---

## 2. 记录什么、谁看得到

### 记录的字段

| 字段 | 来源 | 说明 |
|---|---|---|
| 手机号 | 激活那一屏她自己设 | **只当用户名**（2026-08-30 定）。不用于联系、不入模型提示词、不下发前端、不进日志。找回账号靠它 + 密码，所以**输两遍核对** |
| 编号 `teacher_ref` | 我录名单时分配 | **人**的稳定标识，从 1001 起。换班跨园都不变，研究追人靠它 |
| 姓名 | 合作园给的名单 | 后台需要知道「中二班的哪位老师」才能把积分发对人。一般管理员**只看姓氏** |
| 幼儿园 | 名单 | 后台维护**固定园所列表**，防止「阳光幼儿园/阳光园」写成两个 |
| 年级 / 班级 / 岗位 | 名单 | 年级就是 `age_group`（小/中/大班）；岗位是主班/配班/保育员/园长/其他 |
| 对话与成稿 | 使用中产生 | messages / lesson_plans / lesson_images，已在存 |
| 用量 | 派生 | 不建计数器，直接从事实表 count（计数器会和事实漂移） |

### 三条铁律

1. **姓名和手机号永不下发到前端、永不进模型提示词、永不进日志**。姓名只活在服务端和管理后台；
   手机号连老师自己的（`GET /me`）也不回。
2. **幼儿园管理者（园长、园方任何人）看不到任何老师的数据**。管理后台只有一个账号——开发者本人。园长即使自己也是用户，看到的也只有她自己的东西。这条要在协议和界面上**加粗明示**，因为老师最大的顾虑不是"开发者看到我"，而是"园长看到我写教案偷懒/写得差"——这个顾虑不卸掉，没人敢真用
3. **不存幼儿的任何信息**——这条红线不变，任何时候不动。
   界线：园所档案里的「在园幼儿人数」是机构规模、不指向任何一个孩子，那个可以存；
   按班、按年龄段、按个体拆到能定位到人的一律不存

### 协议（首次登录，激活前必须同意）

要点，全部大白话：

- 采集范围：**手机号、姓名、园所、班级、岗位**（手机号是她自己设的登录名；名单来自合作园）；
  她在平台里的对话、生成的教案与配图、使用量。
  **手机号那条要写明「只当用户名」** —— 别再写回「不存你的手机号」，那是 2026-08-19 的旧文案
- 用途：确认身份、发放额度、研究与产品改进
- **「你的幼儿园和园长看不到这里的任何东西。你的使用记录、写了什么、用了多少次，只有这个项目的团队看得到，不会提供给园方。」**（加粗，原话级别的直白）
  - 2026-08-17 从「只有开发者本人」改成「只有这个项目的团队」：后台加了同事账号之后，
    原来那句话就不准确了。**挂着不准确的隐私承诺，和挂着假承诺是一回事。**
    团队里的一般管理员看不到对话正文和姓名全名（见第 6 节）
- 不收集孩子的任何信息
- 输入和 AI 输出会经过内容安全检查（2026-09-01 起是**阿里云内容安全**，不是微信的）

**协议文案改过三次，三次都是因为它变成了假话** ——
2026-08-17 加了同事账号，「只有开发者本人」不再准确；
2026-08-19 手机号从库里删掉，「你填问卷时留的手机号」那句不准确了；
**2026-08-30 手机号又回来了**（只当用户名），「不存你的手机号」这句**反过来**变成了假话。
**挂着不实的隐私说明比不写更糟**，所以每次动数据模型都要回头看这一页。
现在协议页上写的是「手机号只当用户名」。

---

## 3. 额度：只追加、不修改的台账

### 模型

```
余额 = Σ(发放) − 消耗

发放：quota_grants 表，一行一笔，只插入不修改
      (teacher_id, 文案±N, 配图±N, 原因, 发放时间)
      原因必填："完成8月问卷" / "首次激活" / "反馈奖励" / …

消耗：不记账，从事实表派生 ——
      文案 = count(lesson_plans) + Σ max(0, version − 3)
      配图 = count(lesson_images WHERE status='ready')
```

### 消耗规则（已拍板）

| 动作 | 消耗 |
|---|---|
| 生成一份教案 | 文案 1 次 |
| 改稿第 1、2 次（同一份教案） | **免费** |
| 改稿第 3 次起 | 每次文案 1 次 |
| 生成失败 / 改稿失败 | 不消耗（只数成功的） |
| 配图成功 | 配图 1 张 |
| 配图失败 | 不消耗 |

改稿计费不需要新字段：`lesson_plans.version` 本来就随改稿 +1（初稿 v1，两次免费改到 v3），
**version ≥ 4 的那次开始计费**，公式即 `max(0, version − 3)`。

### 闸门位置

- **开新会话**时查文案余额 ≥ 1。不能等到生成时才拦——让老师答完 4 题再告诉她"额度不够"是恶劣的体验
- **改稿**时若 version 已 ≥ 3（下一版将计费），查余额 ≥ 1
- **配图**时查配图余额 ≥ 1（每日 10 张的防刷上限保留，作为第二道闸）
- 余额不足的提示语自带出路：「本月额度用完了——完成问卷任务可以获得更多，联系发你兑换码的那位」

### 「我的」页展示

剩余文案 N 次 · 配图 N 张，点开看台账明细（哪天、哪个任务、加了多少）。老师能对账，就不会觉得额度是黑箱。

---

## 4. 反馈

当前教师页面在“我的”中提供产品建议，正文经过阿里云文本审核。教案评价的旧接口与历史记录仍保留，但成稿页评价入口已撤掉，不作为当前教师操作步骤。

反馈作为任务奖励时，由运营认定后发放兑换码，教师自行续兑。

---

## 5. 问卷星操作手册

### 5.1 问卷必备字段（每期问卷都要有这组）

| 字段 | 要求 |
|---|---|
| 手机号 | **必填**，开启问卷星的格式校验。两边各存一份，用途不同：问卷星那份用来联系她、发奖励、多期问卷对同一个人；**库里那份只当她登录名** |
| 姓名 | 必填。跟名单核对时用 |
| 幼儿园 | **下拉单选**，选项与后台园所列表一字不差。不要用填空 |
| 班级 | 填空，如「中二班」 |
| 年龄班 | 单选：小班 / 中班 / 大班 |
| 岗位 | 单选：主班 / 配班 / 保育员 / 园长 / 其他 |

研究题项随每期主题变，上面这组身份题**每期固定放最前面**。

⚠️ **库里现在还存手机号了**（2026-08-30 定，只当用户名），所以这一段原来那句
「我们库里不存手机号」已经不成立。两边的分工是：
**问卷星那份用于联系她、发奖励、多期问卷对同一个人；库里那份只用于登录。**

### 5.2 新老师首次接入

1. 后台批量创建兑换码并导出 CSV；外部问卷平台的发码与跳转由该平台配置，本项目不实现奖励页。
2. 后台准备地区、合作园及名单。名单可通过文本或 xlsx 导入，先预览再确认；不导入手机号。
3. 教师获得有效码后打开 `/redeem`，从地区与名单选择自己，或选择「不在名单之内」自填，再设置手机号密码并同意协议。
4. 名单外教师可自行填写身份，不需要补名单才能激活；省市从内置全国地区表选择，不要求当地已有合作园。选错身份需人工核实，不依赖遗留 openid 判定。

问卷星的既有流程是外部配置参考，本次文档整理未核验其当前产品界面。

### 5.3 老老师完成新任务

2026-08-18 改了：**不再「进她的账号页加一笔」，而是再发一个码**。

1. 后台「任务」页发起任务（问卷链接 + 奖励额度 + 截止日期 + 定向），
   老师在小程序首页看到条带
2. 她填问卷 → 我导出答卷核对
3. 后台「批量建码」要 N 个（初始额度就是任务承诺的那个数），
   建完弹框里一行一个列出来，复制或下载 CSV
4. 把码发给完成了的那几位，她在「我的」页点「兑换」输进去

为什么绕这一圈而不是直接加：**匿名码激活的老师在后台没有手机号可搜**，
「找到她」这一步本身就不成立。发码是无状态的——我不需要知道她是谁。
代价是多一次转发动作，换来的是整个流程不依赖「我能认出她」。

### 5.4 纪律

- **每一笔额度必须写原因**，这是研究记录也是对账依据
- 问卷星每期导出的 Excel 存档（本地即可），出争议时是原始凭证
- 码未被兑换超过 30 天 → 微信上提醒一次；老师说不用了 → 作废该码

---

## 6. 管理后台

打开 `http://<域名或 localhost:3000>/admin`。首次启动时系统会用 `.env` 的 `ADMIN_PASSWORD`
自动建一个 `admin` 超级管理员；之后改密码在后台改，那个环境变量就不再起作用。

**当前本地开发密码：`admin` / `123456`**（2026-08-17 用户指定）。
线上部署前必须改掉——后台能看到全部老师的姓名和对话内容，弱密码在公网上等于没有密码。
生产环境启动时若密码短于 12 位，会打印一条醒目的告警。

### 两级权限

2026-08-17 从「只有一个账号」改成多账号，因为同事要帮忙做运营。
这直接触及对老师的承诺，所以权限是这样切的：

| | 超级管理员 | 一般管理员 |
|---|---|---|
| 发额度、建兑换码、园所、反馈 | ✓ | ✓ |
| 老师列表、余额、用量统计 | ✓ | ✓ |
| **姓名全名** | ✓ | ✗ 只看姓氏（`王**`） |
| **园长联系电话全号** | ✓ | ✗ 只看打码 |
| **`claimed_openid`**（历史字段，新 Web 账号不据此判断认领者） | ✓ | ✗ |
| **对话正文与教案内容** | ✓ | ✗ 只看写了几份 |
| **管理员账号管理、操作记录** | ✓ | ✗ |
| 改自己的密码 | ✓ | ✓ |

**为什么这么切**：老师同意的协议里写着「你的幼儿园和园长看不到这里的任何东西」。
同事不是园方，这句承诺依然成立——但原来的措辞「只有开发者**本人**可见」在有第二个人
登录之后就不准确了，所以协议文案同步改成了「项目团队」。

同时把最敏感的两项锁在超管手里：同事做运营根本不需要读老师写了什么。
**每多一个人能读老师写的东西，那句承诺就少一分是真的。**

仍然**没有**「园所管理员」这种角色。要是哪天加了园长只读账号，那句承诺就作废了，
必须先跟老师们重新讲清楚。

### 账号保护

- 不能停用自己（停完就登不进来，得手动改库才能救）
- 最后一个活跃的超级管理员不能被停用
- 密码 scrypt 加盐存储，不存明文
- 登录时用户名不存在也会跑一次哈希比较，防止靠响应时间枚举用户名

### 操作审计

`admin_logs` 表记录发额度、建码、作废、建管理员、改状态等操作。
单人时期这张表没必要（「谁发的」答案永远是「我」），多人之后它是对账的前提——
额度对不上时要能查出是谁在什么时候发的。**审计里不放对话正文，只放操作参数。**

### 页面顺序（2026-08-18 调过）

**园所排在老师前面**，紧跟概览：合作是按园谈的，老师是园带进来的。
原来的顺序（概览 → 老师 → 兑换码 → 园所）反映的是「一个个老师」的视角，
而实际要管的是「哪个园在用、我的钱剩多少、该给谁派任务」。

1. **概览**——① 我的钱（充了/花了/剩多少）② 谁在用（几个园几位老师）
   ③ 哪个园用了多少额度 ④ 等我处理。**删掉了「最近写的」和累计数**
2. **园所**——**特征为主**（省市 / 城乡 / 公办民办 / 教师·幼儿人数 / 联系人），
   用量汇总和编辑收进详情弹窗。特征同时是**任务定向的依据**
3. **名单**——粘一段文本导入（先预览再写）、编号 / 姓名 / 年级班级岗位 / 认领情况、
   「她换班了」、作废。**这是激活的第二把钥匙**
4. **老师**——按姓名/班级/**编号**/**兑换码**搜、详情、停用。**没有发额度**
5. **兑换码**——单个生成、批量建码（建完弹框里一行一个 + 复制 + 下载 CSV）、作废。
   码上**不带任何身份**，只是一张入场券
6. **反馈**——两个 tab：关于产品 / 关于教案质量
7. **任务**——发起活动通知：问卷链接、奖励额度、截止日期、定向（含发布前试算覆盖人数）
8. **模型管理**（超管，2026-08-23 起含文本模型）——文本 / 配图两个 section，
   加/改/删、测试（文本发一条极小消息、配图试画一张）、各设各的默认。
   文本模型带「思考模式」「联网」开关，**真实生效**（落到 API 请求参数），
   该服务商不支持的置灰。最后一个启用的文本模型删不掉、停不了（删光了服务起不来）
9. **管理员**（超管）——建号、重置密码、停用
10. **操作记录**（超管）——带筛选（按人/按动作/按日期）+ 每页 100 条翻页

**老师详情页要回答四件事**（缺哪一件都得跳出去查别的页）：

| 问 | 铺的是什么 | 为什么非要有 |
|---|---|---|
| 她是谁 | **编号**（人）、**位置**（园所班级岗位） | 编号用于追踪教师，位置说明任教情况；手机号仅作登录名。兑换码通过兑换码管理查询 |
| 额度用到哪了 | 剩余/已发/已用 + 台账 | **没有发放表单了**（2026-08-18）：日常任务奖励通过兑换码发放，另有一次性完善信息奖励 |
| 用得怎么样 | 写完几份（只列完成的）、每份出到第几版、画了几张、花了多少 | 配图是成本的主要变量，按人看只有这一处 |
| 她说了什么 | 评价与建议，**点标题直接翻出那一份的正文** | 「大班搭高塔 v2 被标了用不了」，得能立刻看到原文为什么 |

**只列写完的教案**：答题中的草稿不进这张表——那是她被打断留下的半截，
不是「她写过的教案」。库里那些 draft 一行都不动（断点续写依赖它们），只是这个视图不显示。

**「看正文」（超管）**：`GET /plans/:id` 这个接口一直有，但界面上没有入口，等于没有。
现在教案那张表每行一个按钮，弹出教案全文 + 模型自检 + 对话记录。
**能按版本切换**（v1 / v2 / v3 各自的原文和当时那句改稿意见），
对话记录**以 JSON 呈现**，可整块选中复制拿去做研究分析。
这是回答「AI 写的教案到底适不适龄」——这个产品最大的未知数——唯一的办法。

**园所页的重心是特征，不是用量。** 特征不只是档案，它是**任务定向的依据**：
「只发给农村园」「只发给广东的公办园」这些条件全部落在省市 / 城乡 / 办园性质这几个字段上。
用量汇总（老师数、7 天在用、没兑的码、教案数、额度、配图、花费）收进详情弹窗——
一张表塞 15 列谁都读不下来。
点「看老师」跳老师页并预设园所筛选，**不为「这个园的老师列表」再做一份界面**。

`contact_phone` 是园长的号，**一般管理员只看打码**。它是这个库里**唯一还存着的电话号码**
——老师那一列 2026-08-19 删掉了。留着它是因为要联系园所谈合作，而那是机构联系人不是老师。
**有联系人不等于给园方开账号**：仍然没有「园所管理员」这种角色，
那句「你的幼儿园和园长看不到这里的任何东西」依然成立。

---

## 6.5 任务：告诉老师现在有什么可以换额度

额度只走兑换码之后，缺了一环：**她怎么知道现在有个问卷填了能拿 20 次教案**。
以前只能在微信群里喊一声，谁看到谁没看到我这边一无所知。

任务**不自动发额度**。它只说三件事：做什么（问卷链接）、给多少、什么时候截止。
她填完，我在问卷星那边核对，然后建码发给她。
**系统不去猜「她是不是真填了」**——答卷在问卷星，我们库里没有。

**定向**（这是用户明确要的）：向特定地区或特殊群体发。六个维度可叠加——
省份、城市、城乡、办园性质、具体园所、老师年龄班。
空的那一维不限，非空维度之间是「都要命中」。

发布前有个**试算覆盖人数**。这不是锦上添花：条件叠到六层之后不试算没法确认筛对了，
而发错是发给真人的。匹配逻辑只写一次（`services/tasks.js`），
试算和老师端列表共用同一个函数——写两份迟早分叉，
分叉的表现是「后台说发给 12 个人，实际只有 8 个人看到」。

**没有园所的老师**（匿名码激活、没填园所）只匹配「园所相关维度全空」的任务。
这条要记住：否则她永远收不到任何定向任务，而我们不会知道。

老师端入口：**首页一条条带**（有未读才出现，没有就不占地方）+「我的」页一块。
不新增 tabbar——信息架构是定死的（首页只是一个对话框）。
小程序打不开外部网页，所以问卷链接给**复制按钮**，不是一个点不动的链接。

---

## 6.6 平台自己的账

概览要回答「我账面上还剩多少钱」，而这个数以前算不出来，因为两头都缺：

- **收**：没有任何充值记录。我在 DeepSeek / 12ai 充了多少，库里一无所知
- **支**：只有配图成本。**文本成本一分钱都没记**——每次调用的 token 数只进了日志，
  而生成一份教案是最贵的那次调用，漏掉它这个数就是错的

所以 011 迁移建了两张表：`platform_topups`（我手录充值）和 `model_calls`（每次文本调用一行）。

```
账面剩余 = Σ充值 − (Σ配图成本 + Σ文本成本)
```

跟额度台账同一个纪律：**余额是算出来的，不是存出来的**。记错了记一笔负数冲账，不改历史。
单价放 `config.js`，按各家官网当时的价填；`model_calls.cost_cents` 落库时就算好，
**不留到查询时再乘**——单价会变，否则改一个常量会让全部历史悄悄漂移。

**成本数据不完整这件事要在界面上说出来**：早期 12 张图没有成本记录、
文本成本是 2026-08-18 之后才开始记的。不说的话那个「花了多少」会被当成全部历史。

---

## 7. 落库与接口索引

完整数据库结构以 [迁移目录](../../backend/src/db/migrations/) 为准，不能用早期列清单创建当前库。

| 接口 | 用途 |
|---|---|
| `POST /auth/login` | 手机号密码登录 |
| `POST /auth/roster/options` | 持有效兑换码查询身份选项 |
| `POST /auth/activate` | 首次建立账号、身份与额度 |
| `POST /auth/redeem` | 登录后的续兑 |
| `POST /me/agree` | 同意协议 |
| `GET /me/quota` | 余额与台账 |
| `POST /me/profile` | 完善信息并领取一次性奖励 |
| `POST /feedback` | 产品建议 |

详细请求与权限以 [API 约定](api-spec.md) 为准。
