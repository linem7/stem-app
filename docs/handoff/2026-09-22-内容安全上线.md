# 内容安全上线 · 2026-09-22

| 做了什么 | 状态 |
|---|---|
| 阿里云部署 TextModerationPlus，开启 CONTENT_CHECK_ENABLED | 已完成，进程由 92493 重启为 107767，HTTPS healthz 正常 |
| 审核故障暂停提交 | 503 可重试；缺凭据启动失败，不静默放行 |
| 教案审核前不显示正文 | 生成进度保留，标题、正文与结构化内容通过后展示 |
| 长文本与编辑审核 | 重叠分段并避开代理对；去除编辑接口截断，审核合并后完整内容；档案偏好也送审 |
| 图片本身审核 | 尚未接入；目前审核配图描述文字 |

验证：真实阿里云正常输入、AI 输出、过敏提示、违规样本、第二段违规、假凭据均符合预期。18 项离线测试覆盖超时、HTTP/业务失败、畸形响应、风险等级、Unicode 分段、边界、启动失败及流式正文不可见。后端 lint 无错误（13 条既有警告），年龄班、解读、流式、上下文回归通过。

线上 HTTPS 接口验证：正常反馈保存成功；违规反馈、昵称、开场输入均返回 400 安全检查提示，并核实未入库。使用已有合成测试账号；正常反馈测试行已按精确 ID 和正文删除，未改账号额度或协议状态。

部署只同步相关后端文件及审核环境变量，保留线上数据库连接、PUBLIC_BASE_URL 和配图限额。备份 `/root/backups/content-safety-20260922-2046`；其中包含凭据，保持仅 root 可读，禁止提交。没有提交或推送 Git。

用户下一步：刷新页面，用正常主题完成一次教案生成，确认成稿显示和等待体验。启用后只显示生成进度，审核完成后显示正文。

边界：不追溯扫描历史数据；本轮未运行真实模型生成整份教案，不把审核单元/接口验证表述为整条生成链路验收。
