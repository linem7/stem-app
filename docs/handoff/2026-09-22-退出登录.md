# “我的”增加退出登录（2026-09-22）

用户批准执行退出登录计划。退出按钮位于“我的”底部，即使额度或记忆加载失败仍可退出。

- 普通退出不二次确认；有未保存的首页输入、个人档案、记忆、建议、答题或改稿／配图输入时提示，可取消继续编辑。
- 清除当前浏览器 token、登录资料和启动错误，关闭抽屉与“我的”，完整加载登录页释放组件状态、教案缓存、轮询和跨页数据。
- 浏览器从历史快照恢复页面时重新加载校验登录。退出不删除账号、额度或业务数据，不吊销其他设备的登录凭证。
- 请求层按退出次数识别过期响应：退出前的续期结果不能恢复旧 token，旧 401 不能踢掉新账号。
- 原“删除我的全部数据”入口保留，未修改注销业务。

验证：前端 lint、生产构建、test:logout、设计令牌和对比度检查通过。Chromium 1280px、390px 模拟账号测试覆盖退出、刷新、后退、未保存提示取消／确认及换账号隔离；测试不创建或修改真实账户。线上部署结果见本节后续记录。

本轮没有改后端、数据库或账号额度，没有 git commit/push。浏览器测试依赖安装在 /tmp/stem-logout-check；可重复运行的请求竞态测试在 frontend/scripts/logout-test.mjs（npm run test:logout）。

部署完成：前端产物已发布到 `/opt/stem-app/frontend/dist`，先发布新资源再替换入口 HTML，保留旧资源供已打开的页面使用。备份 `/root/backups/logout-20260922-201714`。线上 1280px／390px 模拟账号验证通过，后端健康正常。同一浏览器其他标签页通过 storage 事件同步退出，多标签页测试通过。
