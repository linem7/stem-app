# 幼儿园 STEAM 教案生成平台

帮中国大陆幼儿园老师撰写 STEAM 教案的**微信小程序**。老师说一个想法（"我想做个浮与沉的活动"），AI 一屏问 4 个问题、每题附可点选的推荐答案卡片，最后生成结构完整的教案，可选配图；觉得哪里不对说一句就能让它重写。

**非商用**，作者带着 Claude 独立开发。

---

## 先读这些（按顺序）

| 文件 | 内容 |
|---|---|
| `MEMORY.md` | **协作记忆**：他的偏好、他反悔过什么、我犯过的错、只能他做的事。**这份先读，五分钟** |
| `docs/handoff/` 里**最新那两份** | 上一轮做了什么、哪些坑是真踩过的。按文件名的日期排 |
| `docs/PRD.md` | 产品需求，含信息架构与开发阶段 |
| `docs/adr/ADR-001-technology-stack.md` | 技术选型及**为什么**，含明确不做的事 |
| `docs/design/api-spec.md` | **前后端契约。改接口先改这里，再改代码** |
| `docs/design/db-schema.md` | 六张表设计 |
| `docs/design/design-tokens.md` | **唯一色彩来源**，设计与代码共用 |
| `docs/design/age-band-adaptation.md` | 年龄班规则，产品核心差异化 |
| `docs/design/framework-extraction.md` | 从台湾 STEAM 教材提炼的教学框架 |
| `docs/design/system-prompts.md` | 系统提示词设计 |
| `docs/design/operations.md` | **运营体系**：兑换码身份、额度台账、问卷星流程、反馈、管理后台 |
| `design-demos/direction-approved.md` | 设计方向为什么选 B |

---

## 已定死的决策（不要重新讨论，除非用户主动提）

- **平台**：微信小程序（不做原生 APP）。需 ICP 备案
- **模型**：文本 DeepSeek。图片**多家并存，老师在设置页里选，默认 `gpt-image-2`**（2026-08-18）
  - **加一家不用改代码**：管理后台「配图模型」里填一行配置就行（007 迁移建了 `image_models` 表）。
    三种接口格式已覆盖目前见到的全部——`openai_images`（Bearer + 宽x高）、`gemini`（key 走 URL + aspectRatio/imageSize）、`minimax`；只有冒出第四种格式才需要写适配器
  - **老师完全不碰模型**（2026-08-18 用户定）。加模型、改模型、**设为默认**全在管理后台；
    小程序里一个开关都没有，`GET /me/image-models` 已下线，请求里传 `provider` 后端也不看
    —— 用哪家是技术判断（谁把记录表画对了、谁快、谁便宜），老师没有判断依据，选错了还会怪自己。
    「设为默认」存 `app_settings` 表（008 迁移），**改完立刻生效，不用重启**
  - **默认是 `nanobanana`**（gemini-3.1-flash-image-preview）。实测最快：18 秒，gpt-image-2 要 70 秒，
    记录表和头饰都画对了（头饰的带子终于画到画面边缘）
  - **老师一句话里说了好几样，画在同一张纸上、上下排开**（「小狗、小猫和兔子的头饰」→ 一张纸三条），
    不拆成几张 —— 一份教案总共才 3 张配额。原来会**只画小狗**、另外两样悄悄丢掉，
    因为构图规则里写死了「中间一个图案」（`countSubjects` + `purposeSpec(purpose, count)`）
  - `gpt`：gpt-image-2，经 **12ai 中转**（`https://cdn.12ai.org/v1` + `/images/generations`，一个 Bearer token）。默认用它的理由只有一个但很硬：**记录表**。MiniMax 画表格试了五次五种错法（7列2行还带手写体乱码、3×3、2×3 顶着一行 "Name ______"、5列没横线），而那张图的用途就是印出来给孩子填，格子不对就是废纸；gpt-image-2 第一次就给了干净的 3×4、粗黑线、零文字
  - `minimax`：image-01 留着不删。插画类（材料图、环创背景）它的扁平矢量更贴这套设计，而且一家挂了还有另一家
  - 走 12ai 不等于破了「不用 OpenAI」那条：**用户在大陆，直连 OpenAI 的 IP 不通**，这里连的是国内可达的中转
  - 实测教训见 `backend/src/services/gptImage.js` 的文件头：路径别照抄文档（会变成 `/v1/v1/`）、尺寸不限于文档列的三档（1536×2048 照收，打印分辨率能保住）、只用 `b64_json`（`url` 指向第三方，寿命未知）、慢（medium+1536×2048 要 71 秒）
- **后端**：Node.js 20 + Express + **原生 JavaScript（ESM）**。不用 TypeScript、不用 ORM、不用 Docker、不上微服务和消息队列
- **数据库**：PostgreSQL，`pg` 驱动 + 手写 SQL
- **前端**：uni-app（尚未开始）
- **知识库**：只做框架提炼，**不做全文 RAG**（规避版权）。原书 PDF 不入库
- **语言**：全程简体中文，大陆幼教用语。原书是繁体，输出必须本地化
- **设计方向**：方向 B（Typeform 标杆迁移）。单一焦点列、选项配 A/B/C/D 字母 + 打勾、拇指位常驻操作条。**这些是用户选定的核心资产，不要改**
  - 2026-08-17 用户拆掉了两样原属方向 B 的东西：**左侧竖向序号刻度轨**（每个刻度的位置是绝对定位硬编码，而右边内容高度取决于教案写多长、答了几题，两边永远对不齐）和**一次只问一题**（改成一屏 4 题）。序号仍然保留在题块和记忆行里
- **配色**：用户指定。奶油白底 + 暖阳黄（主行动）+ 薄荷绿（次级/已完成）+ 天空蓝与珊瑚粉（点缀）。见 design-tokens.md
- **信息架构**：首页**只是一个对话框**，不放任何历史入口。过往教案无论完成与否全部进教案库
- **老师不登录，身份就是 openid**（2026-08-19 查清并定死）。`wx.login` 静默给 code → 后端换 openid（`services/wechat.js`）→ `teachers` 用 openid 做唯一键 upsert（`routes/auth.js`）。没有账号密码，她自己也看不见。**换手机 openid 不变；换微信号 = 一个全新账号**
  - **openid 是随机串，微信不告诉我们它属于谁。** 所以「她是谁」必须由别的东西建立 —— 这是兑换码存在的全部理由
  - **首次激活要两样**：**兑换码**证明「你是这批人里的」（问卷星在她提交答卷后当场发，不用人工私发）+ **从名单里选自己是哪一位**（园所 → 班级 → 岗位·姓氏）
  - **库里没有老师的手机号**（016 迁移把 `teachers.phone` 和 `teacher_roster.phone` 都删了）。013 那一版曾经是「让她填手机号跟名单核对」，016 换掉了：11 位手打打错一位是常事，而她分不清是「码坏了」还是「我打错了」；从列表里认自己出错概率低一个数量级；而且她要证明的事情本来就只是「我是阳光幼儿园小一班的主班」，那句话里没有手机号 —— **库里少一样可识别到人的东西，就少一整套合规义务**
  - **要认下来的代价**：从列表里选是一个**表单字段，不是钥匙**，谁有码都能滚到任何一行。所以真正的门槛只剩码那一把。可以接受，因为同事之间冒领没有收益（她自己填问卷也能拿到同样额度），而真会发生的「手滑选错同班另一位」是**可查可改**的 —— 名单那一行记着 `claimed_openid`
  - ⚠️ **拉名单必须先有一个有效的码**（`POST /auth/roster/options` 会先校验）。不设这道门，任何人打开小程序就能看到一整个园的老师名单。**姓名只给姓氏**
  - 🔴 **校验失败绝不能消耗那个码。** 顺序必须是「先全部校验，再全部落库」——她进不来时只会说一句「用不了」，没有别的线索
  - **三层身份，别混**：`teacher_roster.teacher_ref`（**人**，换班也不变）/ `teacher_roster.id`（**位置** = 人×园×班×岗位，就是 class_teacher_id）/ `teachers.id`（**账号**）。追这位老师按 `teacher_ref` 归组，追这个班按（园所 + 班级）归组
  - **她换班了走 `POST /roster/:id/reassign`**：新开一行、沿用同一个 `teacher_ref`、旧那一行标 `moved` **留着不删**（研究要用它区分「她在小一班那半年」和「她在中二班这半年」）。她自己什么都不用做
  - **她注销之后位置放回「等她来认领」**：位置是园所的，不是她的 —— 明年这个班还有主班，只是换了人。但 `claimed_openid` 留着（谁认领过的历史，出争议时要查）
  - **换微信了靠换绑码**（`account_rebinds`，014 迁移），由我在后台发起，一次一码、7 天有效、只能用一次。它挪 openid、保留教案额度记忆和已同意的协议，并**返回新 token**（她手上那个 JWT 指向被删掉的行，不换就 401）
  - **换绑会让旧设备当场失去访问**（`token_version`，015 迁移）。这一条是做完之后测出来才补的：换绑把 openid 挪到**旧那一行**上，所以旧 token 指向的行还在、`status` 还是 active，`requireAuth` 拦不住它 —— 而换绑的常见起因就是手机丢了。**注销没有这个问题**（它把 status 改成 deleted，requireAuth 那道立刻生效）
  - **老师端那个输入框不许把手机号设成必填**：换绑发生在全新微信上，她落在「首次激活」那一屏、手上却是换绑码，没有手机号要填。设成必填按钮永远是灰的，换绑在界面上直接被堵死（第一版就是这么错的）
  - **不做「输手机号自动换绑」**：那等于「知道她手机号 + 有任意一个码 = 接管她账号」，正是名单那套设计要避开的弱点
- **配图是给老师打印出来用的**（2026-08-17 用户定的）。不是拿来看的示意图 —— 这条决定了一切。
  - 五种用途，构图完全不同（`backend/src/services/imagePurpose.js`）：材料图（单件占满画面）、记录表（粗线大格子、格内留白、竖版）、头饰（中间图案 + 左右两条长带，要画到画面边缘才够绕头）、展示图（网格分隔）、环创背景（四周装饰、**中间大片留白**给孩子贴作品）
  - 老师可以从材料清单里挑，**也可以自己描述**（比如背景墙）。入口是底部抽屉，不是让她滚回页面顶部去点
  - 一份教案最多 3 张。尺寸按打印定：长边 2048（A4 约 250 DPI），不是按屏幕
  - 顺带把最难的风险消掉了：这些图里本来就不该有儿童，**「不要画脸」那条压不住的约束现在根本不需要**
- **配图提示词的三条实测教训，别凭感觉改回去**：
  1. **否定约束必须写在英文风格前缀里**。中文那几条只有 DeepSeek 看得到，图片模型从头到尾没见过那段中文 —— 中文写着「一个字都不许画」，出来的记录表照样印着 BOAT / HEAVY STONE 和一个假签名
  2. **文档类图要关掉 MiniMax 的 `prompt_optimizer`**。「可复印的练习纸」这个视觉套路里天然带标题栏和出版社水印，润色器会把它们补回来，盖掉我们写死的禁令。插画类（材料图、背景墙）开着有提升，保留
  3. **只写 centered 不够**，要连着写「不要房间、不要地面、不要墙角、占满画面」。第一版画出来是空墙角里躺着一枚很小的磁铁，主体不到十分之一
- **改稿必须能回退**（2026-08-17 用户提的）。改稿是覆盖式的，不留退路等于逼老师赌一把，她会因此不敢提意见 —— 而「改一改」正是这个产品的核心。
  - 回退**不新增版本号、不删任何版本**，可以来回切；不消耗额度（没退路本来就是我们造成的）
  - **图片永远不跟着版本走**：改稿、回退都不动它。理由是老师的行为本身 —— 她只在觉得某样材料值得画的时候才点生成，那份判断不会因为教案改了一句话就失效。技术上是天然的（图挂 `lesson_plan_id`，那行 id 从不变），**但任何地方都不许按版本清理图片**
- **老师不在成稿页手打改教案**（2026-08-21 用户定）。改教案只有一条路：「改一改」那条 AI 重写。
  手打改教案在手机上本来就难用，而 AI 重写是这个产品的核心 —— 多开一条手改的路，
  等于让最不好用的那条跟核心功能抢位置。
  - `PATCH /lesson-plans/:id` 和它的 `POST .../update` 别名**都通着，只是小程序端没有调用方**。
    这是有意的，不是待补的缺口。别看到「没人调」就把接口删掉
  - 三个 PATCH 接口现在都有 POST 别名（`memories/:id`、`me`、`lesson-plans/:id`），
    因为 **wx.request 发不出 PATCH**。新加 PATCH 接口照这个模式办：
    **同一个 handler 挂两个方法**，不要为小程序单写一份实现 —— 两份里总有一份没人测
- **教案解读只在学习模式有，默认折叠，不进导出**（2026-08-21）。挂 `content_json.commentary`，
  9 个键的白名单，**第三次模型调用**（try/catch，失败只记日志）。
  - **效率模式下连这个键都不下发。** 前端判据只有一个：`commentary` 有没有内容 ——
    **不许再判断一次「是不是学习模式」**，那等于把同一件事记在两个地方
  - 🔴 **别把这三样东西混成一个词，2026-08-21 已经因此来回改过一次**：
    | 说的是 | 字段 | 在哪 | 导出吗 |
    |---|---|---|---|
    | **设计意图** | `content_json.intent` | 教案正文第一节，跟活动目标同级 | **一直都导出** |
    | **教案解读** | `content_json.commentary` | 成稿页各板块**底下**那行折叠的「为什么这样设计」 | **不导出** |
    | 「为什么问这个」 | 出题响应的 `why` | 引导页每题下面 | 不导出，也不落库 |

    用户说「导出时解读也导出」时指的是**设计意图**，而它本来就在导出里。
    **讨论这三样必须带字段名**，光说「解读」会再翻一次车
  - `renderMarkdown(plan, { withCommentary })` 那个开关留着但**没有调用方** ——
    哪天要导，改导出路由一行即可
  - **宁可缺一条，不要凑一条**：模型答不上来的板块就不写那个键。
    同 STEAM 五域那条「宁可诚实标注缺席，不要虚假齐全」

---

## 容易犯的错

**年龄班规则必须真正生效，不能只写在提示词里。** 那本台湾教材的案例默认孩子会言语预测、会读量杯刻度、能小组分工——这是中大班能力。3–4 岁小班做不到。已做三层保障（`backend/src/services/promptBuilder.js`）：
1. 只注入当前年龄班的规则（三个班一起给，模型会挑最丰富的那套，这是给小班写出大班活动的根因）
2. 时长选项由代码从参数表算（选小班界面就没有 45 分钟这个选项）
3. 生成后代码硬校验（小班出现量杯读数/预测/统计图表/缺误食风险 → 记入 `quality_self.age_band_violations`）

**教案产出的是大陆格式，不是那本书的格式**（2026-08-20 改版）。那本台湾教材是**活动实录** ——
记的是活动做完之后回头看当时怎么发生的；而这个小程序解决的是**活动开始之前**。
所以「教学省思（预期与实际的差异）」整节删掉了：活动还没做，那只能是编的。
正文按大陆常见格式：设计意图 → 活动目标（三维各一条）→ 活动重点难点 → 活动准备（经验/物质）
→ 活动过程（导入/展开/结束）→ 活动延伸 → 安全提示；之后才是 STEAM 标注、《指南》指标、教学实例。
**换掉的只是呈现格式，8 个质量维度和年龄班规则全部照旧**。
完整取舍见 `docs/design/lesson-structure-and-modes.md`。

**「能跑起来的脚本才叫回归」**（2026-08-20 查出来两处）。`smoke.js` 当时**根本跑不起来** ——
激活闸门是 08-17 加的、一屏 4 题是 08-17 改的，它两处都没跟上，
`POST /conversations` 直接 403，而 CLAUDE.md 里一直写着「冒烟脚本 npm run smoke」。
更糟的是 `enforceAgeBand` 的反向测试**从来没有脚本**（「用故意违规的样本测过」是某次会话里手动跑的）。
一个空转的校验和一个严格的校验，输出**完全一样**：都是零违规。
现在有 `npm run test:ageband`（23 条，不调模型不花钱，跑完不到一秒），动硬校验必跑。

**白名单和硬校验是同一类风险：生效和不生效，正常输入下输出一模一样。**
教案解读那道白名单（`normalizeCommentary`）就是这样 —— 模型老实只返回那 9 个键时，
有没有过滤看不出任何区别，区别只在它哪天多写一个键的时候出现，
而那一天不会有人在看日志。所以 `npm run test:commentary`（30 条，纯函数）
第 1 节专门喂它白名单外的键。动 `learningMode.js` 或 `renderMarkdown` 必跑。

**报「坏了」之前先确认那东西真的存在。** 2026-08-21 撞到一次：交接文档写着
「档案编辑和成稿编辑**界面上有入口，点了没反应**」，而把两个页面的 `@tap`/`@press`
全列出来核过之后，那两个入口**根本不存在** —— API 层的函数写好了，没有任何页面调用它。
「有入口但点不动」和「压根没入口」要做的事完全不同（前者是修，后者是做），
按前者去修会得到一个谁也看不见的交付。

**小班的 STEAM 不必五域齐全。** 模型天然倾向凑满五个显得完整，会给小班写出"统计浮沉数量并绘制条形图"。规则是 S+A 为主、E 可缺席，**宁可诚实标注缺席，不要虚假齐全**。界面上用虚线框 + 「刻意不做」胶囊 + 一句理由呈现（见 `design-demos/B-v5-states.png` 的 B3 屏）。

**界面上不要写解释性小字。** 这是最常犯的一条（2026-08-18 用户第四次提出）。已经被要求删掉的有：配图卡片的 hint、设置页的隐私块、协议行的副标题、老师详情页的权限说明、新建管理员的角色注释。

写每一句副标题、每一段 note 之前先问一句：**她不看这句话会做错什么吗？** 答不出来就删掉。「为什么这么设计」属于代码注释和设计文档，**不属于界面**——把设计理由印在屏幕上，是拿使用者的注意力替我记笔记。

只有两种小字留得住：
- **边界说明**——「超管可见全号」这类。不说她会以为是 bug 或数据坏了
- **不可逆动作的后果**——注销那两次确认。「不可逆」三个字必须出现在她按下去之前

判断标准不是「这句话对不对」，而是「删掉它，她还能不能把事办成」。能，就删。

**亮色只做底，深色只做字。** 色板里四个亮色明度都在 0.72–0.85，做大面积底配深墨字很好（7–9:1），但做文字色时只有 1.5–3:1，全部不达标。需要彩色文字时用对应的 `-deep` 档。

**「用 deep 档」本身不等于达标**（2026-08-20 量出来的）。旧 `$mint-deep` `#3E8F5D` 配白字只有
**3.97:1**、当绿字压浅绿底只有 **3.57:1** —— 这条规则写在 design-tokens 里一年，
而它保证的那件事从来没成立过。已压暗到 `#327648`；`$ink-3` 也从 `#7A7368` 压到 `#736D62`
（旧值在 `$paper-2` 上 4.38，差一点点不到 4.5，而它是全项目用得最多的辅助文字色）。
**`npm run test:contrast` 现在逐对算 WCAG 比值**，改任何一个令牌值它会立刻红。
色彩规则不能只写成文字 —— 写了一年没被执行的规则等于没有。

**颜色不做状态的唯一载体。** 黄色在奶油底上先天到不了 3:1，草稿/已完成必须同时有文字。
**选中态也算**：选中胶囊的黄底和未选中的奶油底亮度差只有 **1.51:1**，
也就是说「哪个被选中」几乎全靠色相 —— 而色相正是色觉障碍者拿不到的那一维。
原来只有一个字重 600 兜着。现在选中的胶囊一律多一个打勾（教案库两排筛选、建议分类、
评价三选一、配图用途），跟 `s-option` 用同一个 `iconCheck`。

⚠️ 反过来，**装饰性发丝线故意不满足 3:1**（`$rule-2` 压主底只有 1.39:1）。
3:1 管的是「不看它就读不懂内容」的构件，而卡片是靠白底压奶油底区分的，描边只是收个边。
真把每条描边提到 3:1，这套设计会变成一张硬灰格子。`test:contrast` 第 3 节只查
真正承载信息的图形（目前只有进度条的已完成段，3.8:1）。

**字号只有八级，而且必须写 `var(--fs-*)`**（2026-08-20 收口）。梯子的唯一源是
`frontend/src/utils/typography.js`，不在 SCSS 里。收口前有 90 多处硬编码 `font-size`：
25/26/27rpx 这种一像素的区分纯粹是噪音，还有十几处 22rpx = 11px，
**低于设计文档自己定的 12px 下限**。`npm run test:tokens` 第 4 条扫这个。

🔴 **字号档必须内联在 `s-page` 根节点上，不能用类。** 微信自定义组件默认
`styleIsolation: 'isolated'`，`app.wxss` 里的**类选择器进不了组件内部**
（只有标签选择器能穿透，所以 `page {}` 那层可以），而 `s-page` 的根节点正在组件内部。
走类那条路会**静默失效**：编译不报错，H5 预览还是对的（H5 没有样式隔离），
只有微信里「调到特大什么都没变」。第一版就是这么写的，查编译产物才发现。

**生成教案要 15–30 秒，必须异步 + 轮询。** 同步请求在小程序里会超时。而且**必须能离开**——老师在幼儿园随时被叫走，不能要求盯着屏幕等。

**每答一题即落库。** 老师会被打断，进度不能丢。

**API key 只在服务端**，任何情况不下发到小程序。

**老师输入和 AI 输出都要过微信 `msgSecCheck`。** 小程序有 UGC 不做内容安全，审核过不了。

**不存幼儿的任何信息。** 这条永远不变。

禁的是**个体信息**：姓名、生日、照片、家长联系方式、任何一个孩子的能力评价或行为记录。问自己那句老话：这条数据是关于孩子的，还是关于机构/老师的？关于某个孩子的一律不存。

**汇总规模不算个体信息**（2026-08-18 用户确认）：园所档案里的「在园幼儿人数」是一个机构规模数字，不指向任何一个孩子，跟「在园教师人数」同类。它用来判断合作园的体量。**这条界线以后可能被反复问，所以写在这里**——但界线只到这儿：不许出现按班级、按年龄段、按任何维度拆到能定位到人的幼儿数据。

**老师的手机号和姓名——现在要存了**（2026-08-17 用户明确反转旧红线）。这是合作研究项目：老师填问卷换使用权，手机号是对账问卷与小程序账号的业务锚点。但有三条铁律：手机号姓名永不下发前端、永不进模型提示词、永不进日志；**幼儿园管理者看不到任何老师数据**（后台只有开发者一个账号，这条要在协议里加粗明示——老师最怕的是园长看到她用 AI 写教案）；设置页旧隐私文案（"不存手机号"）已是假话，动工时必须重写。全套方案见 `docs/design/operations.md`。

---

## 当前状态（2026-08-22）

### 一览表（每轮交付都要更新这张表）

| 模块 | 状态 | 还差什么 |
|---|---|---|
| 设计文档 / 设计稿 | ✅ 完成 | — |
| 数据库（20 次迁移） | ✅ 完成 | — |
| 后端业务链路 | ✅ 完成 | 导出 docx、云存储 |
| 生成教案 · 年龄班差异 | ✅ 已验证真生效 | **没有真实老师复核** |
| **教案结构（大陆格式）** | ✅ 完成（2026-08-20） | 等真实老师看一份新格式的 |
| **学习模式 · 入口与问题引导** | ✅ 完成（2026-08-20） | 微信端效果**用户还没看过** |
| **学习模式 · 教案解读** | ✅ 完成（2026-08-21） | 微信端效果**用户还没看过**；文案没有幼教老师读过 |
| 改一改 / 版本回退 | ✅ 完成 | — |
| 配图（5 种用途） | ✅ 端到端跑通 | 上线前把 `IMAGE_DAILY_LIMIT` 从 100 调回 10 |
| 身份 · 激活 · 换绑 | ✅ 完成 | 名单导入真实姓名要**先有伦理审查批号** |
| 发任务 | ✅ 完成 | — |
| 额度台账 / 平台自己的账 | ✅ 完成 | 积分转让没想清楚（会动台账模型） |
| 管理后台 | ✅ 完成 | 线上必须改掉 `admin`/`123456` |
| **管理后台 · 第三轮界面** | ✅ 完成（2026-08-21） | 浏览器里量过了；**他还没看过** |
| **管理后台 · 第六轮（21 条）** | ✅ 完成（2026-08-22） | 他看过了，提了 19 条 → 第七轮 |
| **管理后台 · 第七轮（19 条）** | ✅ 完成（2026-08-22） | 浏览器里逐页量过；**他还没看过** |
| **园所 / 名单 xlsx 批量导入** | ✅ 完成（2026-08-21） | 模板下载 + 上传 + 先预览再写 |
| 小程序十一屏 | ✅ 全部接真后端 | — |
| **四态（空/加载/失败/无网）** | ✅ 完成（2026-08-20） | — |
| **对比度收口** | ✅ 完成（2026-08-20） | — |
| **字号档（三档可调）** | ✅ 完成（2026-08-20） | 微信端效果**用户还没看过** |
| 可点击原型 | ✅ 色板已与文档对上 | **四态和字号档没跟**（有意的，见下） |
| 回归脚本 | ✅ 后端 11 个 + 前端 4 个 | `test:import` 47 条；`test:mp` 红 3 条（小程序 me.vue 撞 key，见交接） |
| **演示数据** | ✅ `npm run seed:demo`（2026-08-22） | 6 园所 / 20 位教师 / 20 条任务；`--clear` 清 |
| **H5 实测工作流** | ✅ 用上了（2026-08-21） | 界面的事我能自己量了，配方在那份交接里 |
| **`POST /me/update` 别名 + 个人档案** | ✅ 完成（2026-08-21） | 微信端**他还没确认**对齐与开关 |
| **`POST /lesson-plans/:id/update` 别名** | ✅ 完成（2026-08-21） | 小程序端**故意没有调用方**，见下 |
| 导出 docx | ❌ 没做 | 现在返回 501 |
| 云对象存储 | ❌ 没做 | 图片存本地 `.local-images/`，**只在单机成立** |
| 内容安全 `msgSecCheck` | ❌ **一次都没真调通** | 要真 AppID；本地靠 `CONTENT_CHECK_ENABLED=false` 绕过 |
| ICP 备案 / 平台账号 | ❌ 没办 | **上线的硬门槛，只能用户办** |
| 真实老师复核教案 | ❌ 没有 | **产品最大的未知数** |

### 输出约定

**每轮交付都要给一张 summary table**（做了什么 / 还差什么），然后单独说清「用户需要做什么」。
理由：这个项目的待办分散在六份文档里，而用户是产品负责人不是开发者 ——
他要的是一眼看到全局，不是读一段散文自己拼出结论。上面那张表就是它，**改完东西记得改它**。

其余「怎么跟这个人合作」的事全在 `MEMORY.md`，不要往这儿抄 —— 一件事只写在一处。

### 原型（`prototype/index.html`）的定位

**它是讲设计方向用的，不是第二个实现。** 色板必须跟文档一致
（`test:tokens` 第 2.5 条盯着 —— 加这条之前 `--mint-deep` 和 `--ink-3`
已经手调到跟文档不是一个值了），**但界面细节不必逐轮同步**：
四态、字号档这些 2026-08-20 加在小程序上的东西它没有，这是有意的。
要看现在长什么样，开微信开发者工具看真机。

⚠️ **根目录 `index.html` 里的跳转目标必须写到 `./prototype/index.html` 这个文件**，
不能只写 `./prototype/` 这个目录。web 服务器会自动补 index.html，
**但 `file://` 双击打开时浏览器不会** —— 只会给一个目录列表，还得再手点一次。
这个坑踩过一次。

**已完成**
- 全部设计文档
- 界面设计稿：定稿版 5 主屏（`design-demos/B-v4-final.png`）+ 状态屏与年龄班差异屏 10 屏（`B-v5-states.png`）
- 后端骨架 29 个文件
- **最小链路已跑通**（2026-08-16）：本机 PostgreSQL 17，`001_init.sql` 已对真实库执行，6 表 10 索引全部建成、迁移幂等；后端正常启动；假登录 → 开会话 → 答题 → 生成教案全程通
- **引导 11 题 → 5 题 → 4 题，且一屏全给**（2026-08-17）。判断标准：**只问模型猜不到的，不问模型本来就该会的**。现在只问：年龄班（唯一必答）、教学重点、**场地**、班上的情况。
  - **场地是后加的，很重要**：户外空地搭跷跷板 vs 教室区角做桌面游戏，活动形态完全是两回事，模型只看主题猜不出来
  - **不问时长**：年龄班一定时长就定了（20/30/40），再问是让老师替代码做算术
  - **不再分轮**：老师看不到总量时，答完一题不知道还剩几题，心里没底就容易退出。一屏摊开 + 一条进度更好
  - 一次性出题**不等于**一次性提交：每选一项仍然调一次 answer 落库，她被叫走进度不丢
  - 答题**不限顺序、可覆盖**；换了年龄班会重拉推荐答案（`GET /conversations/:id/questions?age_group=`），已填的答案不动
- **年龄班差异化已验证真实生效**：同一套代码，三个班出三档——小班 15分/3环节/2指标/E 工程诚实缺席，中班 25分/4环节/3指标，大班 30分/4环节/4指标。硬校验 `enforceAgeBand` 用故意违规的样本反向测过，8 条红线全抓得到、超时长能自动收敛——所以"零违规"是真结果，不是校验空转
- **改一改已跑通**：老师对成稿提意见 → AI 提 3 个**新**问题（不重复引导阶段问过的）→ 整份重写、version+1。历史反馈全存在 `conversations.collected.revisions`，每次重写都一并喂回模型，否则它会把上次改好的地方按默认写法改回去
- 冒烟脚本 `npm run smoke [年龄班]`；`node scripts/export-prototype-data.mjs` 重新生成原型数据快照
- **可点击原型**：`prototype/index.html`（单文件无依赖），线上 https://linem7.github.io/stem-app/
  八屏，用的是真实跑出来的三个班问答 + 三份教案 + 一轮真实改稿；改了设计或题目要重跑导出脚本同步它
- **运营体系已落地**（2026-08-17）：002 迁移建了 kindergartens / redemption_codes / quota_grants / feedback 四表，teachers 增 6 列。兑换码激活、协议、额度闸门（开会话/改稿/配图三处）、两类反馈全部端到端跑通。额度**不存 balance 字段**，用「台账 Σ发放 − 事实表数消耗」算，避免两份事实对不上
- **兑换码分两种**（2026-08-18，009 迁移把 phone/real_name 放开成可空）：
  **匿名码**（后台「批量建码」要 N 个，默认 20 教案 / 10 配图，导出 CSV 灌问卷星或整批给园所）
  和**绑定码**（填了手机号才有，老规矩含查重）。
  匿名码的代价写在 api-spec 里：**「问卷答卷 ↔ 账号」的对应关系不在我们库里了**，
  在问卷星那边；后台按手机号搜不到这批老师，改成**按兑换码搜**（老师列表带上她兑的码）
- **注销**（`DELETE /me`，2026-08-18）：**留壳去身份** —— 删对话教案配图记忆和手机号姓名，
  留 id + openid（用来拒绝再次登录）、额度台账、已提交的建议评价。
  真删行会让她再登录变成全新账号，等于「删完还能接着用」。
  拦「不能再用」靠 openid，所以**换个微信 + 同一手机号仍可重新报名** —— 要堵得留手机号哈希，那与「删除全部数据」矛盾
- **管理后台 `/admin`**（电脑浏览器打开）：概览、老师、兑换码、园所、反馈 + 超管专属的管理员与操作记录。与小程序完全隔离——老师的 token 打不开后台，管理员 token 也调不了业务接口
  - **本地开发账号：`admin` / `123456`**（2026-08-17 用户指定）。**线上必须改**——后台能看到全部老师的手机号和对话内容
  - **两级权限**：一般管理员能做全部运营（发额度、建码、看反馈），但**看不到手机号全号和对话正文**。理由不是技术洁癖：老师同意的协议原话是「只有开发者本人可见」，加了同事账号之后那句就不准确了，所以协议改成「项目团队」，同时把最敏感的两项锁在超管手里。每多一个人能读老师写的东西，那句承诺就少一分是真的
  - 仍然**没有**「园所管理员」这种角色。要是哪天加了园长只读账号，那句承诺就作废，必须先跟老师们重新讲清楚
  - 密码 scrypt 加盐存储；不能停用自己；最后一个超管不能被停用；发额度等操作全部进 `admin_logs`
- 回归脚本：`ops-test.mjs`（运营链路）、`admin-test.mjs`（后台）、`roles-test.mjs`（两级权限边界）、`versions-test.mjs`（版本回退与材料图 + **两个 POST 别名**，`--revise` 跑真改稿）。全部**自造隔离数据，可反复跑**
- 🔴 **回归跑一轮就往库里攒一堆垃圾，清它靠 `node scripts/cleanup-test-data.mjs`**
  （不加参数是预览，`--yes` 才真删）。2026-08-21 清过一次：94 个账号→1、63 个园→2、
  1488 条操作记录→33。**跑完回归想看管理端，先跑它**，不然运营页全是
  「模式回归园_27462735」，真数据一条都找不到
  - 预览是**真跑一遍再回滚**（整个清理在一个事务里，没 `--yes` 就 ROLLBACK），
    不是另写一套估算 —— 两套逻辑各写一份迟早分叉，而分叉的表现是
    「预览说删 3 个、实际删了 300 个」，事后才发现
  - 判据一律是**硬证据**（`openid LIKE 'dev\_%'`、脚本的园所命名规律、
    引用的那一行已经不在了），不按姓名猜。唯一的例外是操作记录里
    `create_codes_batch` / `export_codes` 那几种 —— 它们的 `target` 里那个数字
    是**条数不是 id**（`codes:3` ＝「建了 3 个码」），追不到对象，
    只能靠「紧贴着一条已确认的垃圾日志」判同一次脚本运行。注释里写明了
  - **`model_calls` 一行都不删**：回归真调过 DeepSeek、真花了钱，删掉等于把账做平。
    代价是「总支出」比「按园所加总」多出那一截（没有园所可归的那部分）
  - 加了新的回归脚本，要把它建的园所名字加进 `JUNK_KG_NAMES` ——
    漏了不报错，那个园会悄悄留在库里（08-21 就漏了两个）
- **`test:import`（37 条，2026-08-21 新增）**：xlsx 批量导入。查的是**静默失败**那一类 ——
  列头按位置认而不按内容认，错位之后「广东」被当成园所名字建进去，不报错；
  城乡枚举认不出来时静默留空，那个园从此收不到任何定向任务，而这只在园所页看得出来。
  第 2 节还有一条**跨路径断言**：同一份数据走 xlsx 和走粘贴必须得到一样的结果 ——
  哪天有人为 xlsx 单写一份解析器，那一条会红
- **不起服务、不花钱的三个**：`test:ageband`（年龄班硬校验的反向测试）、
  `test:commentary`（教案解读的白名单与截断 + renderMarkdown 不许漏）、
  `test:mode` 的第 3 节（`resolveMode` 收敛乱值）。改对应代码必跑，跑完不到一秒
- ⚠️ **后端没开 `--watch`**（`admin.bat` 里有一整段说为什么，别加回去：
  `--watch` 会在生成过程中重启，把内存里的任务队列杀掉）。
  所以**改了后端代码要重启才生效**。要跑回归但不想动用户那个窗口，
  可以 `PORT=3100 node src/server.js` 另起一个，脚本用 `API_BASE=http://localhost:3100`
  （`mode-test.mjs` 那个用的环境变量名是 `BASE`，不是 `API_BASE`）
- 前端回归四个：`npm run test:api`（用 uni 桩跑真 `src/api`，打真后端）、
  `test:tokens`（六条：三处色值对账、裸 hex、字号梯子对账、SCSS 里不许有 `$fs-`、
  字号必须走 `var(--fs-*)`、**打网络的页面必须处理失败**）、
  `test:contrast`（逐对算 WCAG 比值，把 design-tokens 的色彩规则变成断言）、
  `test:mp`（查「源码没问题、编译成小程序才出事」那一类）
  - ⚠️ `test:mp` **默认查 `dist/dev/mp-weixin`**。刚跑完 `build` 就要
    `node scripts/mp-lint.mjs dist/build/mp-weixin`，否则查的是旧产物、全绿也没意义（踩过）
- 仓库 https://github.com/linem7/stem-app **已公开**。`.env` 和原书 PDF 被 gitignore 挡着，加新文件前留意别把密钥带进去
- **小程序前端已开工，六屏完成**（2026-08-17，`frontend/`，uni-app Vue3 + Vite）：
  首页①、引导 4 题②、生成中③、教案成稿④、待激活⑨、协议⑩。**全部接真后端**，
  唯一假的是登录（没有真 AppID，走后端 `DEV_FAKE_LOGIN`）
  - 生成完**自动跳成稿**，不让老师多点一下
  - 成稿页含：STEAM 五域（缺席的画虚线框 +「刻意不做」胶囊）、版本条与回退、
    配图抽屉、评价
- **教案版本历史与回退已落地**：004 迁移建 `lesson_plan_versions`，`lesson_plans` 增 `current_version`
- **配图按打印用途分五种**：005 迁移给 `lesson_images` 加 `purpose`；
  规则在 `backend/src/services/imagePurpose.js`，注释里是实测教训，别凭感觉改

**未完成**
- ~~MiniMax 余额不足~~ **配图已端到端跑通**（2026-08-17，换了新 key）：提示词翻译 → 调用 image-01 → base64 落地 → 静态路由展示 → 库里记 key/尺寸/成本，全通。约 30 秒一张，1152×864，2.5 分钱
- 导出 docx（TODO，未配置时返回 `NOT_IMPLEMENTED` / 501 / 不可重试）
- 云对象存储（TODO。没配时图片存本地 `.local-images/`，靠 `/local-images` 静态路由提供，**只在单机成立**）
- ~~前端还剩四屏~~ **九屏全部做完**（2026-08-18）：改一改⑤、教案库⑥、我的⑦都已接真后端。
  **设置⑧整个撤掉了**：内容一路精简到只剩三行（协议入口、文字模型、版本），
  为三行内容多一次跳转不值得，于是直接摆在「我的」底部。
  路由、pages.json、页面文件都清了 —— 不留一个点不到的空页
- **隐私说明只有一份，在使用协议里**（不在「我的」也不在别处）。
  它和协议原本逐条重复，而重复的两份迟早不一致，不一致的隐私说明比没有更糟；
  协议才是老师首次进来签的那份知情同意书（`agreed=false` 的落地页），内容归在那里。
  **旧那句「不存手机号」已按实情重写**（现在确实存，用途、去向、不下发前端都写明了）
- **记忆点整行就是改**：一行只有一个动作，就让整行成为那个动作；不显示 `mem_type`
  （「教学信息」之类是给数据库看的）。改的能力靠 `POST /memories/:id/update` 那条别名
- 前端四态与对比度收口还没做
- **小程序发不出 `PATCH` 请求**。方案已定并落地一处（2026-08-18）：
  后端**同一个 handler 挂两个方法**，`PATCH /memories/:id` ＝ `POST /memories/:id/update`，
  小程序走 POST 那条。记忆现在能改了（点整行进编辑）。
  `PATCH /me`（档案编辑）和 `PATCH /lesson-plans/:id`（成稿编辑）仍然卡着 ——
  照这个模式加 `POST .../update` 别名即可，按规矩先改 api-spec
- **uni 给事件处理器的缓存 key 是按 handler 名算的 8 位哈希，只有 256 个桶** ——
  页面一长就会撞（「我的」页一天撞了四次：`97` / `b1` / `c6` / `55`）。撞了微信会把点击派发错人。
  两条应对：① `v-if / v-else` 一对会让两个 handler 落在同一位置，**改用 `v-show`**
  （两块都真实在节点树里，才会分到不同 key）；② 真撞了就**给其中一个 handler 改名**，
  哈希跟着变（实测 `askDeleteAccount` → `onDeleteDataTap`，key 从 `c6` 变 `00`）。
  `npm run test:mp` 第 2 条专查这个，别当误报放过去
- ICP 备案未办、各平台账号未申请
- 内容安全 `msgSecCheck` 因为没有真 AppID，**一次都没有真正调通过**（本地是 `CONTENT_CHECK_ENABLED=false` 绕过的）

**2026-08-19 这一轮做完的**（管理端第二轮 + 激活链路重做）：

- **管理端重心从「一个个老师」挪到「哪个园在用、钱剩多少」**：概览重做（我的钱 / 谁在用 /
  按园所消耗 / 待办）、园所排到老师前面且重心改成特征（010）、名单新增一页（013）、
  反馈拆两个 tab、操作记录加筛选分页、老师详情撤掉发额度、教案历史带版本、对话记录看 JSON
- **额度只走兑换码一条路**。`POST /auth/redeem` 现在管三件事：**首次激活（码 + 从名单里选身份）**、
  续兑（只要码）、换绑（挪 openid + 新 token）
- **手机号从库里彻底删掉了**（016）。名单是一份**岗位清单**，身份三层（人 / 位置 / 账号）
- **平台自己的账**（011）：手录充值 + 支出自动算。文本成本在此之前**一分钱都没落库**
- **换绑**（014 + 015）：她换微信不再是死胡同，教案额度记忆全跟过来，旧设备当场失效
- 回归脚本从 4 个变 6 个：新增 `activation-test.mjs`（激活边界）和 `rebind-test.mjs`（换绑边界）
- H5 预览的 CORS 代理写进了 `frontend/vite.config.js`，不用每次现搭一个临时脚本了
  （配合 `.env.development.local` 里 `VITE_API_BASE=/v1`，那个文件被 gitignore 挡着）

**2026-08-19 第二批做完的**（激活链路重做 + 发任务）：

- **激活改成「码 + 从名单里选自己是哪一位」**，手机号从库里彻底删掉（013–016 迁移）
- **换绑**：她换微信不再是死胡同，教案额度记忆全跟过来，旧设备当场失效
- **发任务**（012 表 + `services/tasks.js`）：六维定向、发布前试算覆盖人数、
  老师端首页条带 +「我的」一行 + 任务页。**匹配逻辑只写一次**，
  管理端试算和老师端列表共用同一个谓词 —— 写两份分叉的表现是
  「后台说发给 12 个人，实际只有 8 个人看到」，而且不报错
- 回归脚本从 4 个变 7 个：`activation-test` / `rebind-test` / `tasks-test`

**2026-08-20 这一轮做完的**（四态 + 对比度 + 字号档）：

- **四态逐屏过了一遍**。新增 `s-skel`（骨架块）和 `s-state`（空/失败/无网三态共用）。
  修掉三处真错：任务页把**失败伪装成空**（显示「现在没有可以做的事」）、
  「我的」失败后显示假的 `0/0 次教案`、兑换页压根没有加载态（await 那段是一屏
  能打字的空表单，随后自己跳走）
- **「无网」第一次成为一个态**（`stores/net.js`）。判据是两个条件同时成立：
  请求没到后端 **且** 系统报告断网 —— 只看前者会把「wifi 正常、后端连不上」画成无网。
  **网一回来自动重来一次**（浏览器里真断真通验过），她不用记得回来点
- **生成页把失败分成两种**：断网走「接着等」（只重新轮询，后端还在写，不花钱、可自动），
  真没写成走「再试一次」（一次真的模型调用，只能她自己按）。
  原来这两条走同一个按钮 = 每次弱网抖一下就白花一次钱重写一遍
- **字号八级梯子 + 三档可调**（标准/大/特大，正文 15/17/20px）。
  139 处 `font-size` 全部收进 `var(--fs-*)`；十几处 11px 提到 12px 下限
- **对比度量出两个不达标的令牌**并压暗（`$mint-deep`、`$ink-3`），
  选中胶囊一律加打勾
- 前端回归从 3 个变 4 个：新增 `npm run test:contrast`；
  `test:tokens` 从 3 条变 6 条（加了字号梯子对账、SCSS 里不许有 `$fs-`、
  **打网络的页面必须处理失败**）

**2026-08-21 这一轮做完的**（两个 POST 别名 + 教案解读）：

- **三个 PATCH 接口现在全部有 POST 别名**（同一个 handler 挂两个方法）。
  `POST /me/update`、`POST /lesson-plans/:id/update` 补齐了 08-18 留的缺口
  - 查出来一件事：交接文档写的「界面上有入口，点了没反应」**不对** ——
    那两个入口在界面上**根本不存在**，`updateMe` / `updateLessonPlan`
    两个函数写好了但没有任何页面调用。**报「坏了」之前先确认那东西真的存在**
  - 顺带查出 `kindergarten_name` 和 `teaching_years` 在此之前**无路可填**：
    激活只从名单写 `kindergarten_id` 和 `age_group`，
    而 `profile_completed = kindergarten_name && age_group` —— 那个标记
    对**任何**老师都恒为 false。它没被当闸门用，所以不是线上故障
- **档案编辑做了界面**：「我的」页点档案整行 → 内联编辑（园所 / 年龄班 / 教龄），
  沿用记忆那套 v-show 排法
- **成稿编辑故意不做界面**（用户 2026-08-21 定）：老师改教案一律走「改一改」。
  手打改教案在手机上本来就难用，而 AI 重写是这个产品的核心。
  接口通着，别看到「没人调」就把它删掉
- **教案解读落地**（学习模式第 3 阶段）：`content_json.commentary`，9 键白名单，
  第三次模型调用，默认折叠，`renderMarkdown` 一个字都不输出。
  真跑过一份中班教案，9 个键全出、质量不是套话（详见 `lesson-structure-and-modes.md`）
- 回归脚本从 9 个变 10 个：新增 `npm run test:commentary`（纯函数、不起服务）；
  `versions-test` 加了第 11 节（两个别名，含「PATCH 也还收」和白名单边界），
  前端 `test:api` 加了 10.5 节（**跑真 `src/api`**，能抓到「哪天被改回 patch()」）

**2026-08-21 第二批**（用户看过之后提的四条）：

- **档案挪到「我的记忆」底下当第一条，不参与编号**。六项：园所 / 年级 / 岗位 /
  最高学历 / 职称 / 教龄。归在那一节是因为那节的副标题就是「写教案时会自动带上」，
  而这六项正是每次都带上的东西 —— 它跟下面的记忆同类，只是有固定格子。
  **不给它编号**：编号是给「一条条攒起来的记忆」的（她会说「第 3 条删掉」），
  档案只有一份、删不掉
  - **018 迁移**加了 `education`（最高学历）和 `professional_title`（职称）。
    不是装饰性档案，是研究要用的**自变量** ——「AI 写的教案对新手和对一级教师，
    帮助是不是同一回事」，教龄答不了这个问题（教龄长不等于职称高）
  - 🔴 **「未评定」是她主动选的值，跟 NULL（没填过）不是一回事**，
    任何地方不许把 NULL 显示成「未评定」。同理 `teaching_years: 0` 是有意义的
    （今年刚入职），别当空丢掉
  - 前端 `me.vue` 那四份选项清单是后端白名单的**副本**，
    改一档要两边一起改（`test:api` 10.5 节把 18 个档逐个发一遍，对不上会红）
- **任务页补了返回键**。原来那一页压根没有 `#top` 插槽 —— 全局是
  `navigationStyle: custom`，`s-topbar` 是唯一的返回入口，她只能靠底部 tab 绕回去
- **生成页两处**：「给我二三十秒」→「等我 20-30 秒」；
  去掉等待中那个「去教案库等」按钮（不该在等待中劝她走，而且这一屏本来就有
  两个离开的出口）。**按钮去掉后 dock 必须跟着收掉**，否则是一条空白横条
- **档案那一行只写「个人档案」四个字，不预览里面填了什么**。原来摊着
  「阳光幼儿园 · 中班 · 主班 · 本科 · 一级教师 · 教龄 5 年」—— 六项连起来在
  记忆那一列里读起来像第七条记忆，而她要做的事就是「进去改」，摊在外面不省一次点击
- **界面上的取消一律写「取消」，不写「算了」**。「算了」有情绪、还含着
  「不重要」的暗示，而她按它的时候多半只是想退出这一步。
  改过五处：档案编辑、记忆编辑、加记忆、删教案确认框、改一改的放弃按钮
- **教案库的时间带上时刻**（「今天 14:05」）。她一天里常连着写两三份，
  三张卡片全写「今天」就分不出哪张是刚才那份 —— 而这一列排序用的正是 `updated_at`，
  日期挡住排序依据，卡片顺序看起来就是乱的
- **导出不带解读**（第 4 条：那是一次用词误会，见上面那张三行表）

**2026-08-21 第三批**（他在微信里看过之后）：

- **「个人档案」跟编号列的左边缘对齐**，不是跟记忆正文对齐。
  编号列定义了这个板块的视觉左边界，档案往右让 44rpx 就成了「缩进的一条」，
  而它明明是这一列的第一条。`.mem--pf` **不许有 padding-left**，
  也别用空 `<text>` 占位重新造出缩进
- **那一行是开关**：点开、再点收起，箭头跟着转。**展开区里没有「取消」** ——
  多一个取消等于给同一件事留两个入口。所以 `onProfileTap` 必须是 toggle，
  写成「只负责打开」的话她点第二下没反应，唯一出路是把表单填完
- 🔴 **界面的事现在能自己量了**，不用全靠他在微信里看：`npm run dev:h5` +
  chrome-devtools，`getBoundingClientRect()` 量坐标、`el.click()` 测交互。
  配方（造已激活账号、塞 token、**必须 reload 不能 navigate 同一个 hash**）在
  `docs/handoff/2026-08-21-界面打磨与浏览器实测工作流.md`。
  H5 仍然仲裁不了「只有微信才出错」那一类，顺序是
  **H5 里量准 → 编译产物核对 → 他在微信里确认一次**

**2026-08-21 第四批**（管理后台，他一次给了 7 个板块的需求）：

- **园所和名单能传 Excel 了**（`services/xlsx.js` + 四个接口）。依赖是 **exceljs**，
  不是 SheetJS 的 `xlsx` —— npm 上那个停在 0.18.5，作者把新版挪去自家 CDN 了
  - 🔴 **xlsx 和粘贴走同一个解析器。** 服务端把工作表拼成「制表符分隔」的文本再喂给
    `parseRoster` —— 那正是从 Excel 复制粘贴时的格式，所以一行都不用改。
    为 xlsx 单写一份的下场是两份分叉，而分叉的表现是
    「粘贴能导进去、上传同一份数据少认出三个人」，**不报错**
  - **按列头认，不按列位认**：模板发下去是要在 Excel 里被人挪列、删列、
    前面插一列序号的。按位置认的话这些操作全变成静默错位（「广东」被当成园所名字）
  - **枚举写歪要整行拒绝**，不许静默留空：城乡和办园性质是定向筛的字段，
    留空 = 这个园收不到任何任务，而这件事只在园所页看得出来
  - **模板必须带示例行**，而且示例的园所名要**一眼是假的**。第一版用了
    「阳光幼儿园」——那正是库里真实存在的园，于是原样导回来被判成重名。
    那次是撞对了，换个环境就会真建出两个名字看起来完全正常的园
  - 文件走 base64 塞 JSON（`express.json` 限 256kb，50 人名单约 27KB），**不引 multer**
- **兑换码列表那一列原来是错的**：显示的是 `code.kindergarten_id`（「这批码打算给哪个园」
  的备注），而不是「谁用了它」。改成显示真正兑走它的那位老师。
  姓名走 `COALESCE(名单那一行, 账号那一行)` —— 早于名单体系的账号 `roster_entry_id` 是空的
- **批量建码不再绑园所**（用户原话「谁持有谁使用，无需设置过多门槛」）。
  单个建码**仍然能选**，那是有用的：整批交给某个园时「发了几个、兑了几个」是跟进依据。
  两处不一样是有意的
- **任务底部改成 取消 / 保存草稿 / 发送**。「试算一下发给几个人」那个按钮撤了，
  但**试算挪进了发送的确认框** —— 定向叠到六层不试算没法确认筛对了，而发错是发给真人的。
  挪进去其实更强：原来那个按钮是可以不点的，现在必然跑一次
- **个人中心挪到左下角，向上弹**（改密码 / 字号 / 基本信息 / 退出）。
  那一行是**开关**，展开区里没有「关闭」—— 跟小程序那个个人档案行同一条
  - **字号三档用 `zoom`，不是改 font-size**：后台的 font-size 全是硬编码 px、
    散在 `index.html` 和 `app.js` 的行内 style 上（九十来处），逐处改 rem 漏一处
    就是「调了没变」且不报错
  - 🔴 **zoom 只加在 `.wrap` / `.login` / `.modal .box` 上，绝不加在 `#app` 或 `body` 上。**
    `.modal` 是 `position:fixed; inset:0` 的遮罩，落在 zoom 过的祖先里会
    放大时溢出、缩小时盖不满屏幕露出一条边。三档都在浏览器里量过遮罩尺寸
- 侧栏「超级管理员」→「后台管理」，「管理员」→「管理员账号」。
  原来那个名字说的是**谁能看**（一个角色），而分组该说的是**这里面是什么**
- **「修改语言」这一项没做**，按「全程简体中文」那条已定死的决策。
  做一个切了没反应的开关比没有更糟

**2026-08-22 第五批**（管理后台第四轮，他给了 8 条界面 + 3 组功能）：

- 🔴 **六个回归脚本原来把 3000 写死、不读 `API_BASE`**（activation / admin / ops /
  rebind / roles / tasks）。所以「另起一个端口跑回归」那条说明**一直是假的** ——
  它们打的是那个跑旧代码的窗口，而数据库是共用的，于是看到的绿是「旧代码 + 新数据」。
  六个都改成读 `API_BASE` 了。**加新脚本照 import-test 那行写。**
  - 顺着这条查出三个先前就存在的缺陷，都是「生效和不生效、正常输入下输出一样」那一类：
    ① 老师**续兑第二个码**之后在列表里出现两次（`LEFT JOIN redemption_codes` 放大了行），
    额度数字还都对，看起来像数据重复而不是查询写错 ——
    改成标量子查询取最近一个，搜索走 `EXISTS` 遍历她全部的码
    （只按最近那个搜的话，拿旧码去对账会搜不到人）；
    ② **没填姓名的人被显示成 `**`**（`surnameOf(null)` 是空串），
    而 `'**'` 是真值，任何 `filter(t => t.real_name)` 都把无名的人算成有名的 ——
    收成一个 `maskName()`，无名回 `null`；
    ③ **同一个人的姓名有两个来源**：列表取名单行、详情取账号行，
    而换班时新开名单行**不回写** `teachers.real_name` —— 列表显示「甲小美」、
    点进详情是 null，两处都不报错
- **「名单」这个顶级页撤掉了**，并进「老师」成一张表（他的判断：那两页重复了）。
  一行 = 一个岗位，激活了的多带账号和额度，五档筛选，导入名单的入口也在这一页
  - ⚠️ **UNION 那半截不是可选的**：早于名单体系的账号 `roster_entry_id` 是空的，
    只查名单它们会**静默消失** —— 而「一个有 21 份教案的账号不在老师列表里」
    是没人会想到去查的。他选的是「合成一张表」，这条保护是我加的
  - `activated` 只看有没有账号，**不许再判一次 `roster_status`**
- **「充了多少 / 还剩多少」整个概念撤掉了**（他定）。概览三张卡全是花费。
  余额那个数**永远不准而且看起来很准**：充值靠手录（漏录一笔就虚高），
  而真实余额在 DeepSeek / 12ai / MiniMax 各自的后台里。
  支出侧留着 —— 它是每次调用当场落库的事实，不靠人记得录
  - `getMoney()` 里 `topup_cents` / `left_cents` 连字段一起删了，
    `admin-test` 有一条**反向断言**盯着它们不许回来：撤掉一个概念最容易留下的
    残骸就是「字段还在、只是没人显示」，而下一个人看到 `left_cents` 会以为它可信
- **兑换码改成一行一次操作**（019 迁移建 `code_batches`）。
  原来一个码一行，而实际动作是「批量建 20 个」——那一次操作摊成 20 行，
  几批混在一起分不出哪 20 个是刚才那批的
  - **不能靠时间戳凑批**：同一秒里可以有两次操作，而两次参数相同的批量建码
    会被合成一批 —— 于是「重新抄录给没收到的人」抄出来的是 40 个码，
    而这个功能存在的全部理由就是那份准确的码单。（019 的**历史回填**用了时间戳，
    因为对老数据没有别的办法，代价只是「历史上某两批显示成一批」）
  - **删操作时已兑的码留着**（他选的）：`batch_id` 是 `ON DELETE SET NULL`。
    代价是「共几张」从此跟库里的码数对不上 —— 那是取舍本身，不是 bug
  - 批次详情是**纯码单，不标哪一张已用**（他明确说不需要）：
    标注反而让人以为「已用的不用抄了」，而没收到的那个人可能正好拿的是已用的那张
  - `GET /codes` 改形状**破了两个回归脚本的契约**（它们靠它查单个码的状态）。
    补了 `GET /codes/items` 按码查 —— 界面没有调用方但**别删**，
    `activation-test` 靠它验「校验失败绝不能消耗那个码」那条红线，
    而那条红线的反面（码被悄悄消耗）在界面上看不出来
- **任务详情页**：覆盖到的人 + 按园所/城乡/办园性质/年龄班四组分布 + 已读。
  列表删掉「发给谁」「看过的」两列。
  🔴 **谓词复用 `buildMatchSql`**，试算 / 老师端列表 / 详情三处同一个函数 ——
  `covers` 两处必须永远相等
- **任务底部三项** 取消 / 保存草稿 / 发送。试算按钮撤了但**试算挪进了确认框**，
  从「可以不点」变成「必然跑一次」
- **个人中心挪到左下角向上弹**（改密码 / 字号三档 / 基本信息 / 退出）。
  字号用 `zoom`，🔴 **只加在 `.wrap` / `.login` / `.modal .box` 上** ——
  加在 `#app` 或 `body` 上，`position:fixed; inset:0` 的遮罩会放大时溢出、缩小时露边
- **园所支持 xlsx 批量导入 + 单个新增给完整表单**；名单也有模板。
  依赖 exceljs（npm 上的 `xlsx` 停在 0.18.5，作者迁到自家 CDN 了）
  - 🔴 **xlsx 和粘贴走同一个解析器**：服务端把工作表拼成制表符分隔的文本再喂
    `parseRoster`。写两份的表现是「粘贴能导进去、上传同一份数据少认出三个人」，不报错
  - **按列头认不按列位认**（模板发下去是要被人挪列的）；
    **枚举写歪整行拒绝**，不静默留空（留空 = 这个园收不到任何定向任务）
  - **模板的示例园所名必须一眼是假的**。第一版用了「阳光幼儿园」——
    那正是库里真实存在的园，原样导回来被判成重名。那次是撞对了，
    换个环境就会真建出两个名字看起来完全正常的园
- **「修改语言」没做**，按「全程简体中文」那条已定死的决策。
  做一个切了没反应的开关比没有更糟

**2026-08-22 第六批**（管理后台第五轮）：

- 🔴 **管理后台一律用规范术语，不用口语**（用户全局要求）。
  改了一百多处：「收了」→停止、「她换班了」→岗位调整、「看码」→查看兑换码、
  「改」→编辑、「等她来认领」→待激活、「谁在用」→使用情况、「老师」→教师……
  - ⚠️ **这条只管管理后台。小程序那一侧保持口语化**——那是设计方向定的，
    老师不是运营人员。哪天要改小程序的语气，得单独说
  - **代码注释不改**：口语化在注释里是资产（「她被叫走进度不能丢」比
    「需保证会话状态持久化」更让人记得住为什么）
- **园所的「新增」和「批量导入」合成一个入口**（照名单那套）：
  下载模板 / 上传 xlsx / 粘贴文本三条路共用一个预览。**粘一行就是单个新增**，
  所以上一轮刚做的那个完整新增表单撤掉了 —— 两个入口做同一件事，
  人得先决定「我这次算新增还是算导入」，而那个判断对她没有意义
  - 园所的文本解析**按列序认**（第一行像列头就按列头认）。
    跟名单不同：名单那几列的内容认得出来（「主班」只可能是岗位），
    而园所的「广东」「广州」「李园长」都是自由文本，按内容分不开
- **筛选挪到列标题上**（教师页的园所/激活状态，反馈页的类型/评价分类/处理状态）。
  筛选控件离它作用的那一列越远，人越要在两处之间来回对
  - 「这一列正在被筛」必须**看得出来**（选中的下拉给底色）——
    否则「表里怎么少了几行」会被当成数据丢了
- **反馈两个 tab 合成一张表**。⚠️ 08-18 拆成两个 tab 的理由是真的
  （两类的列对不上，合并会让每行一半是「—」），所以这次是**解决那个理由**：
  加「类型」列，把「评价等级」和「建议分类」合成一列（它们是同一个语义位置：
  这条反馈的性质），只剩「对应教案」对建议行是空的
- **教师页的激活列只回答激活与否**。原来混了四种值（已激活/未激活/已调岗/已作废），
  后两个说的是**名单那一行的状态**不是激活状态，混在一列里读的时候
  得先分辨「这个词在回答哪个问题」。现在调岗和作废降为小字
- **`npm run seed:demo`**：6 个园所 / 20 位教师 / 20 条任务 / 45 份教案 / 19 条反馈。
  全走真接口（直插库会漏字段，而漏掉的那个字段正是「这一列是空的」的原因）
  - 例外是教案：真生成要调模型、花钱。那部分直插库，
    但 `content_md` 放的是一句**一眼是占位**的话 —— 不编假教案正文，
    这个项目最大的未知数正是「AI 写的教案能不能用」，掺一份假的比空着糟得多
  - openid 用 `dev_demo_`，**`cleanup-test-data` 有一条例外放过它** ——
    演示数据是要留着看的，跑一次回归后台就空了会很莫名。清它用 `--clear`

**2026-08-22 第七批**（管理后台第六轮，他一次给了 21 条界面需求）：

- 🔴 **DATE 列被时区挪了一天，`db/pool.js` 给 oid 1082 设了原样解析。**
  pg 默认把 DATE 解析成「本地时区午夜」的 Date，序列化成 JSON 之后
  东八区变成**前一天**的 UTC 时刻（`2026-09-01` → `"2026-08-31T16:00:00Z"`），
  前端 `.slice(0,10)` 就少一天。
  - 顺带查出**任务的截止日期一直在被改坏**：编辑弹窗把前一天填回输入框，
    一保存就真的存成前一天 —— **每打开保存一次退一天**，全程不报错
  - `import-test` 第 5.5 节盯这条，而且**把修复关掉验证过它会红**
  - ⚠️ 有两处对 deadline 做日期算术（发布闸门、`days_left`），都实测过不受影响
- **概览**：卡片等宽、数字居中；「按园所消耗」那张表撤了（跟园所页重复，
  花费挪进园所列表那一行，含文本成本 —— 它以前只算配图）
- **园所**：列改成 城市 / 地区 / 性质 / 教师数 / 幼儿数 / 联系人 / 花费 / 起始合作；
  「详情」→「编辑」（统计卡撤掉，只剩表单）。
  **020 迁移**加 `cooperation_started_at`，导入模板和解析器带上这一列
  - 「城乡」这个词界面上一律叫**「地区」**。⚠️ 库里那列还叫 `area_type`、
    导入模板的列头还叫「城乡」—— 改列头会让别人手上填好的表突然认不出来
- **教师**：列拆成 编号/姓名/园所/年级/班级/岗位/状态/两个额度/详情，
  **每行只剩一个「详情」**；岗位调整和作废收进详情
  - 🔴 **未激活的行也点得开详情**（读名单那一行，不请求后端）——
    而那两个动作恰恰主要发生在未激活的行上，不给入口等于把它们删了
  - 详情顶部加「个人信息」六格：城市/园所/班级/岗位/最高学历/教龄
- **教案记录**：修订次数给数字（不是「修订 2 次」），时间 `yyyy-mm-dd hh:mm`
- **教案详情**：「模型自检」撤掉（数据照旧写库，撤的是入口）；
  **对话记录卷成问答对**（`content_json` 之外的第三样东西，挂响应的 `transcript`）
  - 实测 1854 字符 → 124 字符。原来每条 assistant 带着那道题的**全部推荐选项**
  - 🔴 **答不上的题也留一行**（`答` 为 null）：「她没答」本身是信号，
    丢掉之后那一屏看起来像她全答了
- **兑换码**：「新建」和「批量建码」合成一个入口，**由数量决定**；不再指定园所。
  `count=1` 时后端把这次操作记成 `kind='single'`（否则列表写「批量建 1 个」）
- **反馈**：汇总表改**纯文本**（胶囊和按钮外框全去掉）；「对应教案」只给标题、不换行
- **列头筛选**：列名和选中值**共用同一个位置** —— 不筛显示列名，筛了显示筛的是什么。
  原来是「类型 / 产品建议」两行，同一件事说两遍
- **任务**：六个定向维度改**折叠式多选**（`.ms` 控件，保持多选）；截止日期框宽度对齐；
  列表列改成 标题/奖励/城市/办园性质/截止/状态；默认 20 条可选 50/100
  - **列表不再逐个试算覆盖人数**（那是一个任务两条查询的 N+1，只为一个不显示的数）
  - 草稿行的「发布」按钮撤了，发布走**编辑 → 发送**（那条路会强制试算一次）
- **管理员**：「显示名称」从界面整个撤掉，一律按用户名认人（库里那列留着，历史记录要它）
- **配图模型**：**内置那两家也能改了**。做法是「用到才抄」——
  第一次改它时把 `.env` 那份（含 key）抄进 `image_models` 表，之后库里那行盖过 `.env`
  - 🔴 抄的时候 **`api_key` 必须一起抄**，否则她改个名字就让所有老师画不出图，还不报错
  - 🔴 `listModels()` 要**按 key 去重**，不然两份同时在列表里，
    而 `pickModel` 取第一个 → 「改完了不生效」，界面上却显示改过了
  - 内置代号**删不掉**（删了只会退回 `.env` 那份，表现成「删了但它还在」）
- **操作记录**：动作收成 **5 组**，时间三档预设（24 小时 / 一周 / 30 天），每页 20
  - 🔴 `system` 是**兜底组**（不属于其他四组的全归它），不是列举 ——
    列举的话新加的动作会同时不属于任何一组，按组筛时一条都查不到而「全部」里又看得见。
    回归有一条「五组之和 = 总条数」盯着
- **个人中心**：只显示账号名；角色写在「基本信息」里；**界面字号三档整个删了**
  （连 `zoom` 机制一起，不留死代码）
- 🔴 **查出一条回归脚本自己的缺陷**：`versions-test` 里
  `pickModel() === cfg.imageProvider` 这条断言，**在任何人点过一次「设为默认」之后必然红** ——
  取值顺序是「后台设的 > .env」，后台设过就不该等于 .env 了，
  它测的东西跟它守护的规则**方向相反**。改成在受控状态下验三档取值顺序
- 🔴 **还有一条断言写在一个进不去的分支里**：我给对话记录加的断言嵌在
  「找得到一位教案用量 > 3 的老师」那个 `if` 里，第一次跑**全绿，其实一条都没执行**。
  已挪出来。跟「六个脚本不读 API_BASE」是同一类：**验证工具本身也要验证**
- **T10「无法重复查看已生成的兑换码」没复现**：接口和按钮都在，五个批次逐个验过
  都能取回码单。最可能是他那个 3000 后端跑的是 019 之前的代码（改了要重启）

**2026-08-22 第八批**（管理后台第七轮，他看过第六轮之后又给了 19 条）：

- 🔴 **查出粘贴导入园所一直是坏的。** 判据是「至少两格能在列头别名表里查到」——
  而一行**完全正常的数据**就能凑到两格（城乡那格填「城市」，同时也是 city 的列头别名；
  备注那格填「备注」）。于是整行被当表头吃掉，报「认不出园所名称这一列」，
  而人只是粘了一行数据。用户报的「解析粘贴结果预览的功能没做」就是这个
  - ⚠️ **只顾一头的修法会把另一头换回来**：加「必须有一格是 name 列头」之后，
    `['省份','城市'] / ['广东','广州']` 这种**缺了名称列的残表头**变成按列序解析 ——
    建出两个叫「省份」和「广东」的园，不报错。回归里本来就有一条盯着那个，
    它当场救了我一次。现在判据是两条择一：有 name 列头，**或**≥60% 的格子都是列头名
  - `import-test` 两个方向各一条断言，**必须一起在**
- **筛选一律纯文本**（用户第三次提）：选中的列头不再变琥珀底。
  那条「正在被筛必须看得出来」的老规则没作废，换了两个不靠颜色的载体：
  ① select 显示的就是选中的值（不筛才显示列名）② **常驻的「清除筛选」按钮**
  （没筛过时是禁用态 —— 位置固定，工具条不会因为筛没筛而抖一下）
- **列宽固定**。这件事有**两个成因，只治一个仍然会动**：
  ① `table-layout:fixed`（`.tbl-fixed`）—— 默认的 auto 按当前这一屏的内容算列宽
  ② `html{scrollbar-gutter:stable}` —— 筛掉几行后滚动条消失，视口宽 15px，列各宽 3px
- **四张汇总表都有「默认 20 / 可选 50、100 / 翻页」**（教师、兑换码、任务、操作记录）。
  前三个前端切片，操作记录走后端 `per`（**白名单收值**，`?per=100000` 是一次全表扫描）
- **概览**：花费和使用情况各占 50% 横向版面；卡片改成**标签在上小字、数字在下大字居中**
  （先知道这是什么，再读那个数）
- **园所**：导入弹窗两个按钮同尺寸（原生 file input 藏起来，用真按钮 click 它）；
  粘贴框的 placeholder 带列标题、底下那段 note 删掉；编辑弹窗去掉「查看该园教师」
- **教师**：年级 / 班级 / 岗位三列补上筛选（**前端筛**，选项从当前数据里现取 ——
  班级名是人填的自由文本，写死一份清单必然对不上）
- **教师详情砍成两块**：个人信息 + 教案列表（默认 10 条 + 翻页）。
  额度三张卡、发放记录、配图用量、反馈记录全撤 —— 额度在教师列表那一行上就有，
  反馈有它自己一整页。**编号和兑换码留着**，那是「她是谁」，不是「兑换情况」
- **反馈删掉「评价 / 分类」列**（问过一次，他确认不补）。
  ⚠️ 代价：**逐条**的「哪位老师把哪份教案评成不可用」从此没地方看
  （教师详情的反馈记录同一轮也撤了），概览上只剩三档汇总数
- **任务**：「详情」整块删掉（他的判断：编辑跟详情是一样的东西）。
  ⚠️ 代价：覆盖人数 / 已读 / 群体特征分布三样跟着没了 —— 那是他 08-21 自己提的，
  问过一次，他确认不要。**发送前的确认框里还会试算一次**，那个没动。
  新增「问卷链接」列（原来挤在标题底下当小字）
- **配图模型不再写死在 .env**：启动时一次性播种进 `image_models` 表，
  之后**只从库里读**，于是每一个模型都能改能删
  - 🔴 为什么不能「用到才抄」（第一版做法）：只要还会读 .env，
    **删掉的下次重启就会自己回来** —— 「删了它还在」比不给删除按钮更糟
  - 🔴 **标记要在插完之后才写**，否则「标记说播过了、库里什么都没有」，配图整个瘫且不自愈
  - 代价：**播种之后改 .env 不再生效**，改模型一律走后台
- 🔴 **又一条回归脚本自己的缺陷**：`versions-test` 会临时改
  「默认配图模型」这个**生产值**（库跟他那个 3000 共用），原来只写不核 ——
  还原写歪或者中间抛异常绕过 finally，结果就是「他精心选的默认被一次回归悄悄换掉」，
  而脚本报全绿。现在还原之后读回来核一遍。
  ⚠️ 这一轮疑似真发生过：跑完之后库里的值从 minimax 变成了 nanobanana
- 🔴 **改配图模型时按一下「保存」会把接口地址清空，两个模型从此画不出图，全程不报错。**
  这一轮真发生了：`GET /image-models` 那时对内置模型一律回 `base_url: ''`，
  于是编辑表单里那一格是空的；用户打开 gpt 和 minimax 各按了一次保存，空串写进了库。
  - **两处都得修**：GET 照实回地址（已修），`POST /image-models/:key/update`
    也补上「地址必须以 http 开头、模型名不能空」的校验 ——
    `POST /image-models`（新建）**一直有**这道校验，而 update **一直没有**，
    典型的「两条路只守了一条」
  - 库里那两行已修好。`versions-test` 里「每个模型都有地址和密钥」那条断言
    是**看门的**，它红过一次而且是真的，别因为偶尔红就放宽它
- **列宽按每一列的实际内容定，不是等宽**（用户第二次提）。
  ⚠️ `.tbl-fixed` 的表**每一列都要写 width** —— 漏掉的那些会**平分余量**，
  于是两个字的「年级」和十个字的「园所」一样宽 484px，而「操作」那一列
  两个按钮挤在 110px 里换行。等宽从来不是目标，**筛选后不变**才是。
  五张表逐列量过 `scrollWidth`，零截断零挤压
- **页面主标题底下那行汇总小字全删了**（用户提，这类要求第七次了）。
  七处：教师「共 14 位，其中 0 位已激活」、园所「筛出 1 个，共 9 个」、
  反馈「共 N 条，M 条未处理」、任务、操作记录、兑换码、配图模型
  - 判据还是那一条：**删掉它，她还能不能把事办成** —— 能。
    个数表里数得出来、页数在翻页条上、「几条未处理」侧栏红点上一直有、
    「用的是哪个模型」表里那个「默认」胶囊已经说了
  - 兑换码和任务那两行的后半截是**写给我自己的备忘**
    （「额度发放的唯一入口」「奖励需人工核对答卷后建码发放」）——
    正是「拿使用者的注意力替我记笔记」那一条，挪进代码注释
  - ⚠️ **只留了管理员账号那一行**：「一般管理员不可查看教师撰写的内容、
    联系电话仅显示打码号码」是**边界说明**，属于两种例外之一 ——
    不说这句，一般管理员看到打过码的电话会以为是数据坏了
- **园所补上筛选**（城市 / 地区 / 性质，前端筛 + 常驻清除筛选）。
  那三个字段正好是任务定向筛的字段，所以「哪些园收得到某个定向」在这一页就筛得出来
- ⚠️ **HTML 注释写在模板字符串里不能用反引号**，这两轮一共踩了六次：
  后台的视图全是模板字符串，注释里一个反引号就把字符串截断，
  报的 `SyntaxError` 指向的行看起来完全正常。改完 `node --check backend/admin/*.js` 过一遍

**下一个里程碑**：导出 docx、云对象存储、ICP 备案、`msgSecCheck` 真调通。

**写前端前必读**：`npm run test:mp` 那几条规则的注释。它们查的是
「源码看着没问题、uni 构建也不报错、只有微信才拒」那一类坑 ——
自定义组件事件名不许叫 `tap`、`<block>` 上不许有 `wx:key`、
v-for 的 key 跨兄弟列表也要唯一。这几条各花了一到三轮才查出来。
**遇到「点了没反应」，第一件事是读 `frontend/dist/dev/mp-weixin/` 下的编译产物，不是读源码猜。**

产品最大的未知数——AI 生成的教案是否真的适龄可用——初步验证是**可用的**：三个班三档梯度稳定，从 11 题一路砍到 4 题，质量没有下降。但样本仍然只有三份，且**没经过真实幼儿园老师复核**。

**「还没有人评价过」这句话以前是错的**（2026-08-18 查出来）：库里其实已经有评价，只是概览那句 SQL 查 `kind = 'rating'`，而真实值是 `'lesson_rating'`，于是那一屏永远显示零。**报「没有数据」之前先确认查的是不是对的字段**——一个 typo 能让唯一重要的指标静静地消失，而且看起来完全正常。

---

## 开发约定

- `npm install` **必须在本地硬盘跑**。项目原在 Google Drive 上，那是虚拟盘，npm 会报 `EBADF`。这是项目从 Drive 迁到 `D:\stem-app` 的原因
- 改接口先改 `api-spec.md`
- 改配色只改 `design-tokens.md`，组件里禁止裸 hex
- 数据库结构改动走 migration 文件，禁止手工改线上库
- 日志不记录对话正文

---

## 待验证的假设

- `age-band-adaptation.md` 里的**小班时长上限和循环次数**是依据发展常识推的，**需要真实小班老师复核**
- 推荐答案的个性化质量依赖模型，需内测验证
- 配图采用率是成本的主要变量，上线后要盯这个数

- **积分转让**（老师把额度给别人）还没想清楚。它会动额度台账的模型：`quota_grants` 现在的语义是「平台发给某位老师」，转让需要第三种记录；而且转让一旦可行，「一个码只能兑一次」就不再是额度的唯一入口

**手机号已经彻底不存了**（2026-08-19，016 迁移删列）。她要证明的事情只是「我是阳光幼儿园小一班的主班」，那句话里没有手机号。要联系她、要发奖励、要多期问卷对同一个人 —— **她的号一直在问卷星那边**，去那边看答卷，不必进这个库。

**以后要是又想收手机号，先读这几条**：

- 微信 `getPhoneNumber` 给的是「已验证的号」，要**非个人主体**（企业/个体工商户）+ 按次付费。个人主体用不了 —— 这正是当初做兑换码的原因
- **让她自己在输入框里打则平台层面零门槛**，但省掉的只有平台那一层
- 🔴 **PIPL 和伦理审查一分都省不掉**：法律只管库里有没有可识别到人的信息，不管这号是谁给的。姓名 + 园所 + 班级已经算可识别，加上手机号只是更硬
- 所以真要收，先有**伦理审查批号**（要发论文，期刊会要，而且事后补不上）+ **协议里那段单独同意**（用途、去向、保存期限、怎么撤回；塞在长协议里蒙过去不算）
- 开发和回归**一律用假数据**，别拿真名单试手
