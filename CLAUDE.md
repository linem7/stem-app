# 开发约定与当前状态

幼儿园 STEAM 教案助手：面向手机浏览器的非商用研究项目。当前实现以工作区源码和迁移为准；线上状态须单独验证，不能用 Git 提交时间推断。

## 阅读入口与维护规则

1. [MEMORY.md](MEMORY.md)：中文沟通及协作偏好。
2. [文档导航](docs/README.md)：按任务查阅规范，不要求每轮通读历史交接。
3. [后端运行说明](backend/README.md)：环境、启动及测试条件。

本文件是**当前功能状态和待办的统一入口**。需求写入 PRD，接口写入 API 约定，历史过程写入带日期的交接；同一事实不在多份入口维护状态表。

## 必须保留的决策

- 手机网页，Vue 3 + Vite + vue-router；后端 Express + 原生 JS ESM + PostgreSQL 手写 SQL。不做小程序、原生 App、PWA、全文 RAG，不自行引入 ORM、微服务或 Docker。
- 手机号只是登录名，密码使用 scrypt。教师响应不返回手机号或真实姓名，这些信息不进模型提示词和日志；超管的授权查询属于单独权限边界。园方没有查看教师数据的账号角色。
- 首次使用需要有效兑换码；可从名单选择或自填名单外信息，码不再限制园所。名单路径从已有园所地区选择；自填路径从内置全国省市表选择。详见 [ADR-004](docs/adr/ADR-004-open-up-the-roster.md)。
- 人、岗位位置、账号分别是 `teacher_ref`、`teacher_roster.id`、`teachers.id`；换班保留历史位置。手机号登录不依赖遗留 `openid`，也不能随文档整理删除遗留数据库列。
- 额度来自台账，避免维护第二份余额；激活校验失败不得消耗兑换码。完善信息奖励通过 `quota_grants.reason='完善信息'` 防重复领取。
- 不收集个体幼儿信息。机构规模汇总与教师主动填写的研究档案另行区分，记忆抽取不得推断姓名、幼儿表现、家庭或健康信息。
- 一屏 4 题，每答一题落库。生成与改稿异步执行、允许离开；断网重连只恢复轮询，不自动再发模型请求。
- 只注入当前年龄班规则，再做生成后硬校验。小班／中班／大班分别为 20／25／30 分钟、3／4／4 环节、2／3／4 指标。小班允许 STEAM 部分领域缺席，禁止为凑齐五域制造超龄活动。
- 配图用于打印，方案卡片一条是一张纸的完整内容；记录表独占一张纸。输入框中老师修改后的描述优先于推荐卡片。具体约束见提示词、教案结构和配图相关交接。
- 模型由管理后台管理，老师不选模型。推断接口格式先看模型 ID 再看地址；修改时无法识别则保留原格式。模型开关必须验证开启与关闭两侧，最后一个启用文本模型不能删除或停用。
- Word 含正文、设计意图和可读取的已完成配图；学习模式折叠解读 `commentary` 不导出。下载直接返回文件，图片存磁盘并压缩，不要求对象存储。
- 内容审核开启时，标题、正文和结构化内容通过审核后才展示；长文本完整分段审核，局部编辑审核合并后的内容。审核异常暂停提交，不能静默放行。

独有的历史理由和排错例子保留在 [整理前开发约定快照](docs/handoff/2026-09-23-整理前开发约定.md)，其中旧状态不作为当前操作依据。

## 当前状态（2026-09-23 文档核对）

| 模块 | 当前工作区实现 | 剩余事项／边界 |
|---|---|---|
| 身份与激活 | 手机号密码登录、名单与自填激活、续兑、完善信息奖励 | 超管重置教师密码／修改登录手机号仍是需求，当前后台未提供对应接口 |
| 登录保持与退出 | token 默认 30 天；`GET /me` 在剩余不足一半时续期；退出清理本机状态并同步同浏览器标签页 | 不吊销其他设备凭证；有未保存输入时可取消退出 |
| 教案与改稿 | 4 题引导、两种模式、异步生成、改稿、版本回退 | 推荐答案个性化继续观察；作者本人负责判断教学适用性 |
| 内容安全 | 阿里云 TextModerationPlus；输入与输出审核、异常暂停、审核前不展示正文 | 图片本身审核未接入；不回溯审核历史数据 |
| 配图与导出 | 打印方案、磁盘压缩、Word 及 Markdown 导出 | 打印类图片压缩质量仍待实际打印确认；观察配图采用率 |
| 教师网页 | 首页、对话流、宽屏侧栏／窄屏抽屉、“我的”、协议、登录与激活 | 侧栏左滑／悬停删除仍待做；`/preview` 临时预览路由仍保留，本轮不改业务代码 |
| 管理与数据 | 两级权限、园所名单、老师、额度、任务、反馈、模型管理；迁移 001–025 | 积分转让尚未定；其他模型格式实际可用性需分别验证 |

## 部署记录与未确认项

以下来自 **2026-09-22 交接记录**，本次文档整理未访问线上核验：

- 教案入口为 `https://115.29.167.18/`，首次使用 `/redeem`，已有账号 `/login`，后台 `/admin/`。IP HTTPS、HTTP 跳转和自动续期已有完成记录；域名购买与备案未记录为完成。见 [HTTPS 交接](docs/handoff/2026-09-22-IP-HTTPS与备案.md)。
- 退出登录前端和内容安全后端各有部署验证记录。见 [退出登录](docs/handoff/2026-09-22-退出登录.md) 与 [内容安全](docs/handoff/2026-09-22-内容安全上线.md)。内容安全交接未宣称真实模型生成整份教案已完成端到端验收。
- 问卷入口 `:8443` 仅准备了返回 503 的占位入口，公网放行及项目部署未确认。见 [双网站端口记录](docs/handoff/2026-09-22-双网站端口隔离.md)。
- 旧交接有数据库密码失败及后续部署连接修复记录；当前工作区连接是否可用未在本轮验证。不要沿用旧指令直接修改数据库密码。
- 实际 `IMAGE_DAILY_LIMIT`、管理员配置和服务环境取部署配置；本轮未读取凭据，也不把历史的 100／默认弱密码当作当前线上事实。

## 开发与验证约定

- 接口改动同步 [API 约定](docs/design/api-spec.md)；数据库改动走 migration，禁止手工改变线上结构。
- 配色以 [设计令牌](docs/design/design-tokens.md) 为唯一来源，不在组件散布裸 hex。界面无意义解释小字不添加；失败态不得画成空数据或零额度。
- 后端默认 3000，与 Vite 代理一致。生成队列在内存，运行任务时禁止自动重启；改后端需确认实际进程重启。纯文档修改无需重启。
- 检查与测试按 [运行说明](backend/README.md#验证与测试) 选择。白名单、审核、硬校验必须覆盖反例；有服务或模型依赖的测试不得冒充离线回归。
- 不主动提交或 push；不使用多 agent，除非用户明确要求。公开仓库不写凭据、真实手机号、教师姓名、可用兑换码或数据备份。
- 交付说明实际改动、验证结果和未完成项，用简表列出，并单独说明用户需要做什么。纯文档整理不为验收创建账号或兑换码。
