# 依赖
node_modules/
**/node_modules/

# 环境变量与密钥（绝对不能提交）
.env
.env.local
.env.*.local
*.key
*.pem

# 构建产物
dist/
build/
unpackage/
.output/

# 日志
*.log
npm-debug.log*
logs/

# 编辑器与系统
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# 原书与研究资料 —— 有版权且体积大（research/ 共 189MB），**不入库**
# 框架已提炼到 docs/design/framework-extraction.md，后续开发用不到原书。
# 挡整个目录，不只挡 *.pdf：2026-08-18 发现里面还有一份别人的教案 docx，
# 只写 research/*.pdf 时它是"未跟踪"状态，git add -A 会把它传到这个**公开**仓库里。
research/

# 临时文件
*.tmp
.cache/

# 原型数据导出的临时目录
.tmpdata/
*.stackdump

# 开发期本地图片落地目录（没配对象存储时用）
backend/.local-images/
.local-images/

# 图片提示词实验产物，不入库
.imgtest/
.tmpimg.jpg
